% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cutoff.R
\name{scr_cutoff}
\alias{scr_cutoff}
\title{Stage 6: cut-off sweep with frozen cuts}
\usage{
scr_cutoff(x, n_cuts = NULL, cuts = NULL)
}
\arguments{
\item{x}{An object from \code{\link[=scr_scorecard]{scr_scorecard()}}.}

\item{n_cuts}{Number of candidate cuts. \code{NULL} uses \code{config$cutoff_n}.}

\item{cuts}{Explicit vector of cuts; overrides \code{n_cuts}.}
}
\value{
An \code{scr_cutoff} object with \code{table} (one row per sample and cut)
and \code{direction}.
}
\description{
For each candidate cut, what happens in each sample: the fraction of the
population on the safe side (approval), the event rate on both sides, the
events avoided (share of events falling on the risky side), the
non-events lost and the KS at the cut. The candidate cuts are quantiles
of the score \strong{on train}, applied frozen to the hold-out: both samples
answer on the same numbers, and the comparison between them measures the
stability of the decision, not a sample difference.
}
\details{
The "safe side" is the high-score side under \code{higher_is_safer} (credit)
and the low-score side under \code{higher_is_riskier} (fraud, propensity).
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
res <- scr_select(scr_demo, "default", config = cfg, drop = "id",
                  date_col = "ref_date")
sc <- scr_scorecard(res)
ct <- scr_cutoff(sc, n_cuts = 10)
ct
st <- scr_strategy(sc, revenue_good = 1080, loss_bad = 4500)
st
rj <- scr_reject(sc)
rj
}
\seealso{
Other stages:
\code{\link[=predict.scr_align]{predict.scr_align()}},
\code{\link[=scr_align]{scr_align()}},
\code{\link[=scr_bin]{scr_bin()}},
\code{\link[=scr_model]{scr_model()}},
\code{\link[=scr_monitor]{scr_monitor()}},
\code{\link[=scr_reject]{scr_reject()}},
\code{\link[=scr_run]{scr_run()}},
\code{\link[=scr_scorecard]{scr_scorecard()}},
\code{\link[=scr_select]{scr_select()}},
\code{\link[=scr_split]{scr_split()}},
\code{\link[=scr_strategy]{scr_strategy()}},
\code{\link[=scr_triage]{scr_triage()}}
}
\concept{stages}
