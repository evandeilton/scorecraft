% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{scr_funnel}
\alias{scr_funnel}
\title{Audit funnel: every input variable and its fate}
\usage{
scr_funnel(x, only_selected = FALSE, cols = "essentials")
}
\arguments{
\item{x}{An object from \code{\link[=scr_select]{scr_select()}}.}

\item{only_selected}{If \code{TRUE}, returns only the approved ones.}

\item{cols}{\code{"essentials"} (default) gives a lean view; \code{"all"} gives
everything; or pass a vector of names.}
}
\value{
A \code{data.table} ordered with the approved first.
}
\description{
The central deliverable. One row per input column, plus one per derived
variable, with the descriptive profile, the verdict of every gate, the
votes of every model and \code{exit_stage}, the exact stage at which the
variable failed. No candidate disappears from the report.
}
\section{Values of \code{exit_stage}}{


\describe{
\item{\verb{00.config}}{Never competed: it was in \code{drop}.}
\item{\verb{01.triage}}{Constant, near-constant, high cardinality, exact
duplicate, missing share above the ceiling, or no signal in the coarse IV.}
\item{\verb{02.binning}}{The binning algorithm failed on this column.}
\item{\verb{03.screening}}{Failed one of the eight admission rules.}
\item{\verb{04.holdout}}{IV dropped out of sample, unstable PSI, or part of
the hold-out falls in no bin.}
\item{\verb{05.correlation}}{Redundant with a better-ranked variable.}
\item{\verb{05b.derived_excluded}}{Passed everything, but is a column the
pipeline created and \code{allow_derived_final = FALSE}.}
\item{\verb{06.consensus}}{Not enough votes, or outside the top-N.}
\item{\verb{07.approved}}{Entered the shortlist.}
\item{\verb{08.manual_drop}}{In the automatic consensus, but removed by the
analyst in the coarse classing lab (\code{\link[=scr_classing_choose]{scr_classing_choose()}}).}
}
}

\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
res <- scr_select(scr_demo, "default", config = cfg, drop = "id",
                  date_col = "ref_date")
head(scr_funnel(res, only_selected = TRUE))
table(scr_funnel(res, cols = "all")$exit_stage)
}
\seealso{
Other accessors:
\code{\link[=scr_gains]{scr_gains()}},
\code{\link[=scr_leakage]{scr_leakage()}},
\code{\link{scr_result}},
\code{\link[=scr_score_gains]{scr_score_gains()}},
\code{\link[=scr_score_metrics]{scr_score_metrics()}},
\code{\link[=scr_selected]{scr_selected()}}
}
\concept{accessors}
