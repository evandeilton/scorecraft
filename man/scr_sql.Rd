% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sql.R, R/capital.R, R/ead.R, R/lgd.R, R/pd.R
\name{scr_sql}
\alias{scr_sql}
\alias{scr_sql.scr_capital}
\alias{scr_sql.scr_ead}
\alias{scr_sql.scr_lgd}
\alias{scr_sql.scr_pd}
\alias{scr_sql.scr_result}
\alias{scr_sql.scr_scorecard}
\title{Production SQL}
\usage{
scr_sql(x, table = NULL, dialect = NULL, file = NULL, ...)

\method{scr_sql}{scr_capital}(
  x,
  table = NULL,
  dialect = NULL,
  file = NULL,
  level = c("exposure", "portfolio"),
  ...
)

\method{scr_sql}{scr_ead}(x, table = NULL, dialect = NULL, file = NULL, ...)

\method{scr_sql}{scr_lgd}(x, table = NULL, dialect = NULL, file = NULL, ...)

\method{scr_sql}{scr_pd}(x, table = NULL, dialect = NULL, file = NULL, ...)

\method{scr_sql}{scr_result}(x, table = NULL, dialect = NULL, file = NULL, output = NULL, ...)

\method{scr_sql}{scr_scorecard}(
  x,
  table = NULL,
  dialect = NULL,
  file = NULL,
  what = c("score", "woe", "all"),
  keep_columns = NULL,
  ...
)
}
\arguments{
\item{x}{An object from \code{\link[=scr_select]{scr_select()}}, \code{\link[=scr_scorecard]{scr_scorecard()}}, \code{\link[=scr_pd]{scr_pd()}},
\code{\link[=scr_lgd]{scr_lgd()}}, \code{\link[=scr_ead]{scr_ead()}} or \code{\link[=scr_capital]{scr_capital()}}.}

\item{table}{Source table name. \code{NULL} uses \code{config$sql_table}.}

\item{dialect}{Dialect (\code{"ansi"}, \code{"databricks"}, \code{"spark"}, \code{"hive"},
\code{"mysql"}, \code{"mariadb"}, \code{"sqlserver"}, \code{"bigquery"}, \code{"postgres"},
\code{"oracle"}, \code{"snowflake"}, \code{"redshift"}, \code{"duckdb"}, \code{"sqlite"}).
\code{NULL} uses \code{config$sql_dialect}.}

\item{file}{Path to write to. \code{NULL} (default) returns the lines.}

\item{...}{Passed on to the methods.}

\item{level}{For \code{scr_capital}: \code{"exposure"} (default, one row per
exposure with \code{el}, \code{k}, \code{rw}, \code{rwa}) or \code{"portfolio"} (the aggregate
by segment).}

\item{output}{For \code{scr_result}: \code{"woe"}, \code{"bin"} or \code{"both"}. \code{NULL} uses
\code{config$sql_output}.}

\item{what}{For \code{scr_scorecard}: \code{"score"} (default: the three blocks, with
the points per variable, the exact score and the whole-points score),
\code{"woe"} (the WOE/BIN SQL of the scorecard variables only) or \code{"all"}
(the three blocks plus, for every variable, its bin label, WOE and
points side by side: the deployment layout that reports the band of
each variable next to the score).}

\item{keep_columns}{For \code{scr_scorecard}: key columns carried untransformed
into the output (for example the customer identifier and the reference
date); \code{NULL} uses \code{config$sql_keep_columns}.}
}
\value{
A character vector with the SQL (invisibly, when \code{file} is given).
}
\description{
Code ready to run in the database, covering \strong{exactly} the approved
variables (or those of the scorecard), in blocks in this order:
}
\details{
\enumerate{
\item CTE \code{base_scr}: reproduces the Stage 1 pre-processing -
imputation of missing and sentinel by the \strong{training} median,
special-population flags, \code{COALESCE} of the categorical missing.
\item The WOE/BIN transformation, emitted by
\code{\link[OptimalBinningWoE:obwoe_sql]{OptimalBinningWoE::obwoe_sql()}} from the authoritative cut points
with full precision.
\item (Scorecard) CTE \code{woe_scr} with WOE and bin index, followed by the
final \code{SELECT} with \code{score} (exact, \code{a + b * logit}), \verb{<f>_points}
per variable and \code{score_points} (whole points).
}

The order matters: without the first block, the WOE would be applied to
data different from what was binned. The score computed by the SQL
matches \code{\link[=scr_apply]{scr_apply()}} numerically, by an automated test that runs both
paths.
}
\section{IRB models}{


\code{scr_pd} wraps the scorecard SQL in a common table expression and adds a
\code{CASE} on the score cut points that yields \code{grade} and \code{pd_final}.
\code{scr_lgd} chains the driver bins of both stages, the logits, the pool
\code{CASE} and the floored result. \code{scr_ead} computes the utilisation and the
undrawn amount, assigns the pool from the frozen cut points and applies
the greatest of the model, the drawn amount and the standardised floor.
\code{scr_capital} carries the constants of every pool (PD, LGD, \code{k}, risk
weight) in a \code{pool_params} table joined on segment and grade, so no
normal quantile is evaluated at run time; \code{level} chooses the exposure
or the portfolio output.
}

\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
res <- scr_select(scr_demo, "default", config = cfg, drop = "id",
                  date_col = "ref_date")
cat(head(scr_sql(res, table = "prd.customers", dialect = "databricks"), 20), sep = "\n")
sc <- scr_scorecard(res)
cat(tail(scr_sql(sc), 12), sep = "\n")
}
\seealso{
Other production:
\code{\link[=predict.scr_align]{predict.scr_align()}},
\code{\link[=scr_apply]{scr_apply()}},
\code{\link[=scr_export]{scr_export()}},
\code{\link[=scr_monitor]{scr_monitor()}},
\code{\link[=scr_monitoring_plan]{scr_monitoring_plan()}},
\code{\link[=scr_reasons]{scr_reasons()}}
}
\concept{production}
