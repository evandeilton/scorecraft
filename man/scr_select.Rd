% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{scr_select}
\alias{scr_select}
\title{Select variables for the scorecard}
\usage{
scr_select(
  data,
  target,
  config = scr_config(),
  drop = character(),
  date_col = config$oot_date_col,
  event_level = NULL,
  export = NULL,
  copy = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with the target, the candidates
and, if any, the date column of the out-of-time cut.}

\item{target}{Name of the target column (0/1, logical, or a two-level
factor/character).}

\item{config}{An object from \code{\link[=scr_config]{scr_config()}}.}

\item{drop}{Columns that are never candidates. They stay in the funnel as
\verb{00.config}.}

\item{date_col}{Date column of the out-of-time cut. Defaults to
\code{config$oot_date_col}.}

\item{event_level}{Which target value counts as the event; see \code{\link[=scr_split]{scr_split()}}.}

\item{export}{Directory to write the deliverables to. \code{NULL} (default)
writes nothing; use \code{\link[=scr_export]{scr_export()}} later.}

\item{copy}{If \code{TRUE} (default), works on a copy of \code{data}.}
}
\value{
An object of class \code{scr_result}. Read it with \code{\link[=scr_selected]{scr_selected()}},
\code{\link[=scr_funnel]{scr_funnel()}}, \code{\link[=scr_gains]{scr_gains()}}, \code{\link[=scr_sql]{scr_sql()}}, \code{\link[=scr_leakage]{scr_leakage()}} and
\code{\link[=summary]{summary()}}; continue with \code{\link[=scr_scorecard]{scr_scorecard()}}; write it with \code{\link[=scr_export]{scr_export()}}.
}
\description{
Shortcut that chains \code{\link[=scr_split]{scr_split()}}, \code{\link[=scr_triage]{scr_triage()}}, \code{\link[=scr_bin]{scr_bin()}} and
\code{\link[=scr_model]{scr_model()}} on a table and a binary target, and returns an object with
the shortlist, the complete audit funnel, the gains table and the
production SQL of the approved variables. Every stage remains callable on
its own for whoever wants more control (hybrid interface, decision D13).
}
\section{Reproducibility}{


With the same \code{data}, the same \code{target} and the same \code{config$seed}, the
result is identical with one or several \code{nthread}: the seed governs the
random split, the cross-validation, the classifier subsample, the trees
and the bootstrap, and the binning is deterministic per column.
}

\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
res <- scr_select(scr_demo, "default", config = cfg, drop = "id",
                  date_col = "ref_date")
res
scr_selected(res)
head(scr_funnel(res, only_selected = TRUE))
}
\seealso{
\code{\link[=scr_run]{scr_run()}} for several targets straight from the database,
\code{\link[=scr_scorecard]{scr_scorecard()}} for the next step.

Other stages:
\code{\link[=predict.scr_align]{predict.scr_align()}},
\code{\link[=scr_align]{scr_align()}},
\code{\link[=scr_bin]{scr_bin()}},
\code{\link[=scr_cutoff]{scr_cutoff()}},
\code{\link[=scr_model]{scr_model()}},
\code{\link[=scr_monitor]{scr_monitor()}},
\code{\link[=scr_monitoring_plan]{scr_monitoring_plan()}},
\code{\link[=scr_reject]{scr_reject()}},
\code{\link[=scr_run]{scr_run()}},
\code{\link[=scr_scorecard]{scr_scorecard()}},
\code{\link[=scr_split]{scr_split()}},
\code{\link[=scr_strategy]{scr_strategy()}},
\code{\link[=scr_triage]{scr_triage()}}
}
\concept{stages}
