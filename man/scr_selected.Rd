% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{scr_selected}
\alias{scr_selected}
\title{Variables approved for the scorecard}
\usage{
scr_selected(x, which = c("final", "consensus", "manual"))
}
\arguments{
\item{x}{An object from \code{\link[=scr_select]{scr_select()}}.}

\item{which}{\code{"final"} (default), \code{"consensus"} or \code{"manual"} (\code{NULL}
when no manual choice was made).}
}
\value{
A character vector of column names.
}
\description{
The final shortlist, in consensus order (the first is the strongest). It
is exactly the list \code{\link[=scr_sql]{scr_sql()}} covers and \code{\link[=scr_scorecard]{scr_scorecard()}} fits.
}
\details{
After \code{\link[=scr_classing_apply]{scr_classing_apply()}} the result carries two lists: the automatic
consensus and the analyst's final choice; \code{which} picks one, and the
default is the final one so that every downstream function follows the
analyst's decision.
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
res <- scr_select(scr_demo, "default", config = cfg, drop = "id",
                  date_col = "ref_date")
scr_selected(res)
}
\seealso{
Other accessors:
\code{\link[=scr_funnel]{scr_funnel()}},
\code{\link[=scr_gains]{scr_gains()}},
\code{\link[=scr_leakage]{scr_leakage()}},
\code{\link{scr_result}},
\code{\link[=scr_score_gains]{scr_score_gains()}},
\code{\link[=scr_score_metrics]{scr_score_metrics()}}
}
\concept{accessors}
