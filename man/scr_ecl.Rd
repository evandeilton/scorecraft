% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecl.R
\name{scr_ecl}
\alias{scr_ecl}
\title{Expected credit loss with stage allocation}
\usage{
scr_ecl(
  pd_term,
  lgd,
  ead,
  eir = 0,
  stage = NULL,
  dpd = NULL,
  pd_orig = NULL,
  scenarios = NULL,
  weights = NULL,
  prepay = NULL,
  rho = 0.15,
  t_max = NULL,
  segment = NULL,
  id = NULL,
  config = scr_config(),
  keep_rows = FALSE
)
}
\arguments{
\item{pd_term}{Marginal monthly PDs: a matrix \verb{n x T}, or a vector of
length \code{n} (a flat hazard recycled over \code{t_max} months), or a single
number.}

\item{lgd, ead}{Vectors of length \code{n} (or one) or matrices \verb{n x T}.}

\item{eir}{Annual effective interest rate, vector of length \code{n} or one.}

\item{stage}{Optional stage vector (1, 2, 3); \code{NULL} applies the rule.}

\item{dpd}{Days past due (the rule); optional.}

\item{pd_orig}{12-month PD at origination (the rule); optional.}

\item{scenarios}{Named list of scenario shocks (see Details); \code{NULL}
runs the base case only.}

\item{weights}{Scenario weights; equal when \code{NULL}.}

\item{prepay}{Monthly prepayment hazard: \code{NULL}, a vector or an \verb{n x T}
matrix.}

\item{rho}{Asset correlation used by scenario shocks with \code{z}.}

\item{t_max}{Term in months when \code{pd_term} is a vector (default
\code{config$ecl_horizon_months}).}

\item{segment}{Optional character vector of length \code{n} for a segment
summary.}

\item{id}{Optional identifier vector of length \code{n}.}

\item{config}{An \code{\link[=scr_config]{scr_config()}} object (\verb{ecl_*} keys, \code{verbose}).}

\item{keep_rows}{Keep the per-exposure table.}
}
\value{
An object of class \code{scr_ecl}: a list with \code{exposures} (only
with \code{keep_rows = TRUE}: \code{id}, \code{segment}, \code{stage}, \code{ead}, \code{pd_12m},
\code{pd_life}, \code{ecl_12m}, \code{ecl_life}, \code{ecl}), \code{stages} (\code{stage}, \code{n},
\code{ead}, \code{ecl_12m}, \code{ecl_life}, \code{ecl}, \code{coverage}), \code{segments} (when a
segment is given), \code{scenarios} (\code{scenario}, \code{weight}, \code{ecl_12m},
\code{ecl_life}, \code{ecl}), \code{totals} (\code{n}, \code{ead}, \code{ecl_12m}, \code{ecl_life}, \code{ecl},
\code{coverage}, \code{share_stage2}, \code{share_stage3}), \code{horizon}, \code{t_max},
\code{discount}, \code{stage_rule}, \code{ledger} and \code{config}.
}
\description{
Discrete-time expected credit loss of every exposure:
}
\details{
\deqn{ECL_H = \sum_{t=1}^{H} S(t-1)\, h_t\, LGD_t\, EAD_t\, (1+r)^{-t/12}, \qquad S(t) = \prod_{s \le t}(1 - h_s - p_s)}

with \code{h_t} the marginal monthly default hazard, \code{p_t} an optional
prepayment hazard and \code{r} the annual effective interest rate
(\code{config$ecl_discount = "none"} switches the discounting off). The
12-month figure uses \code{H = config$ecl_horizon_months}, the lifetime figure
the full term \code{T}. Stage 1 exposures carry the 12-month loss, stages 2
and 3 the lifetime loss; stage 3 exposures are credit-impaired and carry
\code{LGD_1 * EAD_1}. When \code{stage} is \code{NULL} the rule is: stage 3 if \code{dpd >= config$ecl_stage_dpd[2]}, stage 2 if \code{dpd >= config$ecl_stage_dpd[1]} or
the 12-month PD now over the one at origination (\code{pd_orig}) is at least
\code{config$ecl_sicr_ratio}, else stage 1.

Scenarios are a named list of shocks applied to the base inputs, each a
list with any of \code{pd_mult} (multiplier of the hazards, capped at one),
\code{z} (systematic factor of the one-factor model applied to the hazards
with correlation \code{rho}, negative in a bad year), \code{lgd_add} (added to the
LGD) and \code{ead_mult}; \code{weights} (normalised to one) give the
probability-weighted result.
}
\examples{
cfg <- scr_config(verbose = FALSE)
d <- scr_demo_portfolio
h <- 1 - (1 - d$pd)^(1 / 12)       # flat monthly hazard from the annual PD
e <- scr_ecl(h, d$lgd, d$ead, eir = d$eir, dpd = d$dpd, pd_orig = d$pd_orig,
             t_max = 36L, segment = d$segment, config = cfg)
e
e$stages
}
\references{
International Accounting Standards Board (2014). \emph{IFRS 9 Financial
Instruments}, section 5.5 and paragraphs B5.5.1-B5.5.55.
}
\seealso{
Other irb-capital:
\code{\link[=scr_capital]{scr_capital()}},
\code{\link[=scr_el]{scr_el()}},
\code{\link[=scr_irb_rw]{scr_irb_rw()}},
\code{\link[=scr_pd_stress]{scr_pd_stress()}},
\code{\link[=scr_sa_rw]{scr_sa_rw()}}
}
\concept{irb-capital}
