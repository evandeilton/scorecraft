% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{scr_connect}
\alias{scr_connect}
\title{Connect to a database (ODBC DSN or any DBI driver)}
\usage{
scr_connect(dsn = NULL, driver = NULL, timeout = 20, ...)
}
\arguments{
\item{dsn}{Name of the DSN configured on the system. Ignored when
\code{driver} is given.}

\item{driver}{Optional DBI driver object, used instead of ODBC.}

\item{timeout}{Connection timeout in seconds (ODBC only).}

\item{...}{Extra arguments passed on to \code{\link[DBI:dbConnect]{DBI::dbConnect()}} (for example
\code{dbname = ":memory:"} for SQLite).}
}
\value{
A DBI connection. Close it with \code{\link[DBI:dbDisconnect]{DBI::dbDisconnect()}}.
}
\description{
With \code{dsn}, a thin wrapper around \code{\link[DBI:dbConnect]{DBI::dbConnect()}} over \code{\link[odbc:odbc]{odbc::odbc()}}
with one deliberate choice: \code{bigint = "numeric"}. Under the \pkg{odbc}
default a BIGINT column arrives as \code{integer64}, and \code{is.numeric()} of an
\code{integer64} is \code{FALSE}: typing would treat the column as a categorical of
very high cardinality. With \code{driver}, any DBI driver object is accepted
(e.g. \code{RSQLite::SQLite()}, \code{duckdb::duckdb()}), which is how the database
path is tested without a DSN.
}
\examples{
\dontshow{if (requireNamespace("RSQLite", quietly = TRUE)) withAutoprint(\{ # examplesIf}
con <- scr_connect(driver = RSQLite::SQLite(), dbname = ":memory:")
d <- scr_demo; d$ref_date <- as.character(d$ref_date)   # SQLite has no Date type
DBI::dbWriteTable(con, "dtm", d)
dt <- scr_fetch(con, "dtm", sample_frac = 0.5, seed = 42)
nrow(dt)
DBI::dbDisconnect(con)
\dontshow{\}) # examplesIf}
}
\seealso{
Other database:
\code{\link[=scr_fetch]{scr_fetch()}}
}
\concept{database}
