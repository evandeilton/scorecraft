% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/default.R
\name{scr_default}
\alias{scr_default}
\title{Build the default flag from a monthly panel}
\usage{
scr_default(
  data,
  id,
  date,
  dpd = NULL,
  arrears = NULL,
  exposure = NULL,
  utp = NULL,
  restructured = NULL,
  obligor = NULL,
  config = scr_config()
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table}, one row per \code{id} and \code{date}.}

\item{id, date}{Column names of the unit identifier and the month.}

\item{dpd}{Column name of days past due (integer). Optional when \code{utp}
is given.}

\item{arrears, exposure}{Column names of the overdue amount and the total
exposure, both optional; when given, the materiality test applies.}

\item{utp}{Column name of a logical unlikeliness-to-pay flag, optional.}

\item{restructured}{Column name of a logical distressed-restructuring
flag, optional.}

\item{obligor}{Column name of the obligor when \code{id} is a facility,
optional; enables the pulling effect.}

\item{config}{A \code{\link[=scr_config]{scr_config()}}; keys \verb{default_*}.}
}
\value{
An object of class \code{scr_default}: \code{flags} (\code{id}, \code{date},
\code{default} 0/1, \code{event_id}, \code{trigger}, \code{months_in_default}, \code{cured}),
\code{events} (one row per event: \code{event_id}, \code{id}, \code{start}, \code{end},
\code{trigger}, \code{cured}, \code{months}), \code{summary}, \code{ledger} and \code{config}.
}
\description{
Applies the standard definition of default to a panel with one row per
unit (\code{id}) and month (\code{date}): a unit enters default when \code{dpd} reaches
\code{default_days} and the arrears are material (both \code{default_abs} in
currency units and \code{default_rel} as a share of \code{exposure}, when arrears
and exposure are supplied), or when \code{utp} (unlikeliness to pay) is \code{TRUE}.
It leaves default after \code{default_probation} consecutive months without a
trigger (\code{default_probation_restructured} when \code{restructured} was \code{TRUE}
at any point of the event). With \code{obligor} supplied and
\code{default_level = "obligor"}, a unit whose obligor has more than
\code{default_pulling} of its exposure in default is pulled into default too,
and a defaulted obligor defaults all its units.
}
\details{
Rows must be monthly; gaps are tolerated (the probation counts observed
months). The result keeps the row-level flags: they are the product.
}
\examples{
d <- scr_default(scr_demo_panel, id = "id", date = "ref_date", dpd = "dpd",
                 arrears = "arrears", exposure = "exposure",
                 restructured = "restructured",
                 config = scr_config(verbose = FALSE))
d
head(d$events)
}
\seealso{
Other irb-parameters:
\code{\link[=scr_default_rate]{scr_default_rate()}},
\code{\link[=scr_irb_params]{scr_irb_params()}}
}
\concept{irb-parameters}
