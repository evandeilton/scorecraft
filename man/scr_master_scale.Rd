% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd.R
\name{scr_master_scale}
\alias{scr_master_scale}
\title{Master scale of PD grades}
\usage{
scr_master_scale(
  pd_min = 3e-04,
  pd_max = 0.3,
  n_grades = 10L,
  method = c("geometric", "supplied"),
  grades = NULL,
  labels = NULL
)
}
\arguments{
\item{pd_min, pd_max}{PD midpoints of the first and the last grade.}

\item{n_grades}{Number of grades.}

\item{method}{\code{"geometric"} (default) or \code{"supplied"}.}

\item{grades}{For \code{"supplied"}: a numeric vector of midpoints or a
\code{data.frame} with \code{pd_lo} and \code{pd_hi}.}

\item{labels}{Optional grade labels (default \code{"1"}, \code{"2"}, ...).}
}
\value{
A \code{data.table} of class \code{scr_master_scale} with \code{grade}, \code{label},
\code{pd_lo}, \code{pd_mid}, \code{pd_hi}, and the attributes \code{ratio} (the geometric
ratio between consecutive midpoints) and \code{method}.
}
\description{
A grade structure with geometric midpoints and geometric-mean boundaries:
\deqn{PD_k = PD_1 \, r^{k-1},\quad r = (PD_K / PD_1)^{1/(K-1)},\quad
      \mathrm{bound}_k = \sqrt{PD_k \, PD_{k+1}},}
so that every grade doubles (or multiplies by \code{r}) the PD of the one
before. With \code{method = "supplied"} the table comes from the user: a
numeric vector of midpoints (boundaries derived as the geometric means)
or a \code{data.frame} with \code{pd_lo} and \code{pd_hi} (and optionally \code{pd_mid},
\code{label}). Grade 1 is always the safest.
}
\examples{
ms <- scr_master_scale(0.0005, 0.25, n_grades = 8)
ms
scr_master_scale(method = "supplied", grades = c(0.001, 0.01, 0.05, 0.20))
}
\seealso{
Other irb-pd:
\code{\link[=predict.scr_grades]{predict.scr_grades()}},
\code{\link[=predict.scr_pd]{predict.scr_pd()}},
\code{\link[=scr_calibrate]{scr_calibrate()}},
\code{\link[=scr_grades]{scr_grades()}},
\code{\link[=scr_migration]{scr_migration()}},
\code{\link[=scr_moc]{scr_moc()}},
\code{\link[=scr_pd]{scr_pd()}},
\code{\link[=scr_pd_pit_ttc]{scr_pd_pit_ttc()}},
\code{\link[=scr_pd_validate]{scr_pd_validate()}}
}
\concept{irb-pd}
