% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lgd.R
\name{scr_lgd_validate}
\alias{scr_lgd_validate}
\title{Validation battery of an LGD model}
\usage{
scr_lgd_validate(x, newdata = NULL)
}
\arguments{
\item{x}{An \code{\link[=scr_lgd]{scr_lgd()}} object.}

\item{newdata}{\code{NULL} (the hold-out), an \code{\link[=scr_workout]{scr_workout()}} object or a
table with the drivers, \code{lgd_real}, \code{ead} and the default date.}
}
\value{
An object of class \code{scr_lgd_validation}: \code{calibration} (per
pool), \code{portfolio}, \code{discrimination}, \code{stability} (\code{pools}, \code{drivers}),
\code{homogeneity}, \code{heterogeneity}, \code{summary} (test, statistic, p, light),
\code{sample}, \code{n}.
}
\description{
Runs the three blocks of the usual LGD validation on the hold-out sample
(or on \code{newdata}) against the training reference:
}
\details{
\itemize{
\item \strong{Calibration.} Per pool and for the portfolio, the one-sided t-test
of realised against estimated LGD (the pool long-run average), where
under-estimation is the failure: \code{p = 1 - Phi(t)}; the loss shortfall
\verb{1 - sum(LGD_real E) / sum(LGD_pred E)}; the coverage of the realised
mean by the downturn LGD; the regression of realised on predicted.
\item \strong{Discrimination.} Somers' D / generalised AUC of the prediction with
its bootstrap interval, compared with the training value through
\code{S = (gAUC_init - gAUC_curr) / sigma_curr}; Spearman rho; the loss
capture ratio; R-squared.
\item \strong{Stability.} PSI of the pool distribution and of the bins of every
driver of both stages, with the fixed and the n-adjusted threshold.
\item \strong{Pools.} Homogeneity within a pool (Welch test between the halves of
the pool split at its median prediction; a small p-value means a pool
that still discriminates) and heterogeneity between adjacent pools
(Welch test; a large p-value means pools that do not differ).
}

Traffic lights use the p-value thresholds of \code{pd_lights} (red below the
first, amber below the second) and the fixed PSI thresholds.
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, n_boot = 20)
wo <- scr_workout(scr_demo_lgd, scr_demo_lgd_cashflows, rates = scr_demo_rates, config = cfg)
m <- scr_lgd(wo, drivers = c("product", "ltv", "prior_dpd_max"), config = cfg)
v <- scr_lgd_validate(m)
v
v$calibration
}
\seealso{
Other irb-lgd:
\code{\link[=scr_elbe]{scr_elbe()}},
\code{\link[=scr_lgd]{scr_lgd()}},
\code{\link[=scr_lgd_downturn]{scr_lgd_downturn()}},
\code{\link[=scr_lgd_floor]{scr_lgd_floor()}},
\code{\link[=scr_lgd_pools]{scr_lgd_pools()}},
\code{\link[=scr_workout]{scr_workout()}}
}
\concept{irb-lgd}
