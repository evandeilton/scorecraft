% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scorecraft-package.R
\docType{package}
\name{scorecraft-package}
\alias{scorecraft}
\alias{scorecraft-package}
\title{scorecraft: scorecard engine with alignment, cut-off strategy and production SQL}
\description{
A professional scorecard is born of seven chained stages, and this package
exposes each of them as a function of its own, next to the shortcut
\code{\link[=scr_select]{scr_select()}} that chains them for the common case:
}
\details{
\enumerate{
\item \strong{Split} (\code{\link[=scr_split]{scr_split()}}): train/hold-out by whole periods
(out-of-time) before any supervised fit.
\item \strong{Triage} (\code{\link[=scr_triage]{scr_triage()}}): structural filters, decomposition of
sentinels and missing values, exact duplicates. The data leaves with no
\code{NA}.
\item \strong{Binning and screening} (\code{\link[=scr_bin]{scr_bin()}}): optimal bins parallelised
by column, eight admission rules, hold-out revalidation with frozen
bins, redundancy pruning.
\item \strong{Multi-strategy selection} (\code{\link[=scr_model]{scr_model()}}): elastic net, boosting
and random forest on the WOE space; consensus weighted by hold-out Gini.
\item \strong{Scorecard} (\code{\link[=scr_scorecard]{scr_scorecard()}}): logistic regression on the
shortlist, sign check, points per bin, a tree challenger explicitly
without points.
\item \strong{Alignment} (\code{\link[=scr_align]{scr_align()}}): log-odds regression on the raw score
composed with the PDO map, with \code{odds_orientation} recorded. Runs
automatically inside \code{\link[=scr_scorecard]{scr_scorecard()}}.
\item \strong{Cut-off and strategy} (\code{\link[=scr_cutoff]{scr_cutoff()}}, \code{\link[=scr_strategy]{scr_strategy()}},
\code{\link[=scr_reject]{scr_reject()}}): sweep with frozen cuts, bands with marginal expected
profit, honest reject inference through a sensitivity band.
}

The deliverables (\code{\link[=scr_export]{scr_export()}}) are the audit funnel, the gains tables,
the production SQL (\code{\link[=scr_sql]{scr_sql()}}) with R-SQL equivalence verified by test,
and four \code{.xlsx} workbooks. \code{\link[=scr_monitor]{scr_monitor()}} recomputes PSI/CSI on new data,
with both the fixed and the sample-size-adjusted threshold, and never
schedules anything by itself.
}
\section{IRB risk parameters}{


The IRB layer turns the scorecard into regulatory parameters and keeps
the same contracts (one configuration, ledgers, hold-out revalidation,
workbooks, production SQL). \code{\link[=scr_irb_params]{scr_irb_params()}} holds every regime-specific
number as a table selected by preset (\code{"bcb"}, \code{"basel3_final"},
\code{"crr3"}); \code{\link[=scr_default]{scr_default()}} builds the default flag from a monthly panel and
\code{\link[=scr_default_rate]{scr_default_rate()}} the default rates by cohort with the long-run average.
PD: \code{\link[=scr_calibrate]{scr_calibrate()}} anchors the scorecard to a central tendency,
\code{\link[=scr_grades]{scr_grades()}} cuts the score into rating grades, \code{\link[=scr_moc]{scr_moc()}} and
\code{\link[=scr_pd]{scr_pd()}} add the margin of conservatism and the floor, and
\code{\link[=scr_pd_validate]{scr_pd_validate()}} runs the calibration, discrimination and stability
tests with traffic lights. LGD: \code{\link[=scr_workout]{scr_workout()}} discounts recovery cash
flows into realised LGD, \code{\link[=scr_lgd]{scr_lgd()}} fits the cure and severity stages
and the pools, \code{\link[=scr_lgd_downturn]{scr_lgd_downturn()}}, \code{\link[=scr_lgd_floor]{scr_lgd_floor()}} and \code{\link[=scr_elbe]{scr_elbe()}}
complete the estimate. EAD: \code{\link[=scr_ead_data]{scr_ead_data()}} builds the realised
conversion factors from facility snapshots and \code{\link[=scr_ead]{scr_ead()}} the pools.
\code{\link[=scr_el]{scr_el()}}, \code{\link[=scr_irb_rw]{scr_irb_rw()}}, \code{\link[=scr_capital]{scr_capital()}} and \code{\link[=scr_ecl]{scr_ecl()}} compute
expected loss, risk weights, capital and expected credit loss. Binning
against a continuous target goes through \code{\link[=scr_bin_continuous]{scr_bin_continuous()}}, whose
result the engine reproduces in R and in SQL.
}

\section{Parallelism}{


Column-wise work (binning, hold-out revalidation, CSI) and the bootstrap
run on \code{config$nthread} workers. The backend follows
\code{getOption("scorecraft.parallel")}: \code{"fork"} on unix by default, \code{"psock"}
on Windows (and selectable anywhere, e.g. for tests), \code{"serial"} to switch
parallelism off. Results are identical across backends.

Forked workers are clones of the parent and, because the garbage collector
writes to the objects it marks, each one ends up owning a copy of most of
the parent heap. On Linux the number of fork workers is therefore capped
at \code{getOption("scorecraft.fork_mem_fraction", 0.75)} of the memory
available divided by the resident size of the session, with a message
when the cap applies. Set the option to \code{Inf} to disable it.
}

\section{Reading conventions}{


\code{objective} declares the vocabulary and the direction of the scale
(\code{"risk"}: more points, safer; \code{"propensity"}: more points, more likely)
and does \strong{not} change what is modelled. \code{event_level} changes what is
modelled. Both are documented in \code{\link[=scr_config]{scr_config()}} and \code{\link[=scr_split]{scr_split()}}.
}

\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/evandeilton/scorecraft}
  \item Report bugs at \url{https://github.com/evandeilton/scorecraft/issues}
}

}
\author{
\strong{Maintainer}: Jose Evandeilton Lopes \email{evandeilton@gmail.com} [copyright holder]

Authors:
\itemize{
  \item Jose Evandeilton Lopes \email{evandeilton@gmail.com} [copyright holder]
}

}
\keyword{internal}
