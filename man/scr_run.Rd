% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{scr_run}
\alias{scr_run}
\title{Run the selection for several targets straight from the database}
\usage{
scr_run(
  con,
  table,
  targets,
  config = scr_config(),
  drop = character(),
  date_col = config$oot_date_col,
  event_level = NULL,
  sample_frac = 1,
  max_rows = NULL,
  export = NULL
)
}
\arguments{
\item{con}{A DBI connection, from \code{\link[=scr_connect]{scr_connect()}}.}

\item{table}{Table name, with an optional \code{{target}}.}

\item{targets}{Vector with the names of the target columns.}

\item{config}{An object from \code{\link[=scr_config]{scr_config()}}, used for every target.}

\item{drop}{Columns that are never candidates.}

\item{date_col}{Date column of the out-of-time split, passed on to
\code{\link[=scr_select]{scr_select()}}. \code{NULL} uses \code{config$oot_date_col} (a random stratified
split when that is \code{NULL} too).}

\item{event_level}{Passed on to \code{\link[=scr_select]{scr_select()}}.}

\item{sample_frac}{Sampling fraction. A scalar or a list named by target.}

\item{max_rows}{Row cap per target. \code{NULL} switches it off.}

\item{export}{Root output directory; each target writes to a subdirectory.}
}
\value{
An \code{scr_runset} object: a named list of \code{scr_result} (or, for the
targets that failed, a list with \code{error}).
}
\description{
For each target: fetches the table, runs \code{\link[=scr_select]{scr_select()}} and writes the
deliverables. A failure on one target is recorded and the loop continues.
}
\section{Table convention}{


\code{table} accepts the \code{{target}} placeholder, replaced by the lower-case
target name. Without the placeholder, the same table is used for every
target.
}

\examples{
\dontshow{if (requireNamespace("RSQLite", quietly = TRUE)) withAutoprint(\{ # examplesIf}
con <- scr_connect(driver = RSQLite::SQLite(), dbname = ":memory:")
d <- scr_demo; d$ref_date <- as.character(d$ref_date)   # SQLite has no Date type
DBI::dbWriteTable(con, "dtm", d)
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
rs <- scr_run(con, "dtm", targets = c("default", "churn"), config = cfg,
              drop = c("id", "ref_date", "default", "churn"))
rs
scr_compare(rs)
DBI::dbDisconnect(con)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=scr_compare]{scr_compare()}}, \code{\link[=scr_core]{scr_core()}}

Other stages:
\code{\link[=predict.scr_align]{predict.scr_align()}},
\code{\link[=scr_align]{scr_align()}},
\code{\link[=scr_bin]{scr_bin()}},
\code{\link[=scr_cutoff]{scr_cutoff()}},
\code{\link[=scr_model]{scr_model()}},
\code{\link[=scr_monitor]{scr_monitor()}},
\code{\link[=scr_monitoring_plan]{scr_monitoring_plan()}},
\code{\link[=scr_reject]{scr_reject()}},
\code{\link[=scr_scorecard]{scr_scorecard()}},
\code{\link[=scr_select]{scr_select()}},
\code{\link[=scr_split]{scr_split()}},
\code{\link[=scr_strategy]{scr_strategy()}},
\code{\link[=scr_triage]{scr_triage()}}
}
\concept{stages}
