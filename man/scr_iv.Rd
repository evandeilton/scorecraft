% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{scr_iv}
\alias{scr_iv}
\title{Information Value of any grouping}
\usage{
scr_iv(g, y, laplace = 0.5)
}
\arguments{
\item{g}{Group vector (any coercible type). For a character or factor
\code{g}, \code{NA} forms a group of its own; for integer codes, \code{NA} rows are
dropped.}

\item{y}{0/1 outcome vector.}

\item{laplace}{Smoothing constant added to each count. \code{0} switches it off.}
}
\value{
Total IV, a scalar. Zero when fewer than two groups are populated.
}
\description{
Laplace smoothing by default. It is not cosmetic: without it, a
single-class group (a normal situation in a small sentinel population)
yields \code{Inf} and contaminates any ordering that depends on the IV.
}
\details{
Implemented with \code{\link[=tabulate]{tabulate()}} on integer codes rather than \code{data.table}
aggregation: this function is called once per candidate variable, hundreds
of times per run, and the fixed cost dominated the triage.
}
\examples{
set.seed(1)
y <- stats::rbinom(1000, 1, 0.3)
g <- ifelse(stats::runif(1000) < 0.5 + 0.3 * y, "A", "B")
scr_iv(g, y)
}
\seealso{
Other metrics:
\code{\link[=scr_metrics]{scr_metrics()}},
\code{\link[=scr_psi]{scr_psi()}}
}
\concept{metrics}
