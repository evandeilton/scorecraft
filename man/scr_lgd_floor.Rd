% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lgd.R
\name{scr_lgd_floor}
\alias{scr_lgd_floor}
\title{Input floor on the downturn LGD per pool}
\usage{
scr_lgd_floor(
  x,
  params = NULL,
  asset_class = NULL,
  secured_share = NULL,
  collateral = c("real_estate", "financial", "receivables", "other_physical")
)
}
\arguments{
\item{x}{An \code{\link[=scr_lgd]{scr_lgd()}} object.}

\item{params}{An \code{\link[=scr_irb_params]{scr_irb_params()}} object; \code{NULL} uses the configured
framework. Edits are detected and recorded in the ledger.}

\item{asset_class}{Row of \code{params$lgd_floor}; \code{NULL} uses
\code{capital_asset_class} of the configuration.}

\item{secured_share}{Secured share of the exposure in \verb{[0, 1]}: one value
or one per pool. \code{NULL} means unsecured.}

\item{collateral}{Column of \code{params$lgd_floor} for the secured part:
\code{"real_estate"}, \code{"financial"}, \code{"receivables"} or \code{"other_physical"}.}
}
\value{
The \code{scr_lgd} object with \code{floors} (\code{table} per pool with
\code{lgd_dt}, \code{floor_unsecured}, \code{floor_secured}, \code{secured_share},
\code{floor}, \code{lgd_final}, \code{binding}; \code{asset_class}, \code{collateral},
\code{framework}, \code{params_modified}, \code{binding_share}) and the pool columns
\code{floor} and \code{lgd_final} updated.
}
\description{
Applies the LGD input floor of the framework's parameter table, blended
between the unsecured and the collateralised floor with the secured
share of the exposure:
\deqn{\mathrm{floor} = \mathrm{floor}_U\,(1 - s) + \mathrm{floor}_S\,s,\qquad
      \mathrm{LGD}^{\mathrm{final}} = \max(\mathrm{LGD}^{DT}, \mathrm{floor})}
A missing unsecured floor (residential mortgages, whose floor applies to
the whole exposure) uses the collateral floor throughout; an asset class
with no floor at all yields a floor of zero.
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, n_boot = 20)
wo <- scr_workout(scr_demo_lgd, scr_demo_lgd_cashflows, rates = scr_demo_rates, config = cfg)
m <- scr_lgd(wo, drivers = c("product", "ltv", "prior_dpd_max"), config = cfg)
m <- scr_lgd_floor(m, asset_class = "retail_other", secured_share = 0.4)
m$floors$table
}
\seealso{
Other irb-lgd:
\code{\link[=scr_elbe]{scr_elbe()}},
\code{\link[=scr_lgd]{scr_lgd()}},
\code{\link[=scr_lgd_downturn]{scr_lgd_downturn()}},
\code{\link[=scr_lgd_pools]{scr_lgd_pools()}},
\code{\link[=scr_lgd_validate]{scr_lgd_validate()}},
\code{\link[=scr_workout]{scr_workout()}}
}
\concept{irb-lgd}
