% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split.R
\name{scr_split}
\alias{scr_split}
\title{Stage 0: type the data and split train and hold-out}
\usage{
scr_split(
  data,
  target,
  date_col = NULL,
  ratio = 0.3,
  seed = NULL,
  event_level = NULL,
  drop = character(),
  copy = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with the target and the candidates.}

\item{target}{Name of the target column. Binary: 0/1, logical, or a
two-level factor/character.}

\item{date_col}{Date column of the out-of-time cut. \code{NULL} uses a random
stratified split.}

\item{ratio}{Target hold-out fraction.}

\item{seed}{Seed of the random split. \code{NULL} leaves the random number
generator untouched, so the split is reproducible only through the
\code{seed} of \code{\link[=scr_config]{scr_config()}}.}

\item{event_level}{Which target value counts as the event. \code{NULL} uses
the convention (\code{1}, or the second alphabetical level).}

\item{drop}{Columns that are never candidates (identifiers, sibling
targets, free text). They stay in the funnel as \verb{00.config}.}

\item{copy}{If \code{TRUE} (default), works on a copy of \code{data}. \code{FALSE}
modifies \code{data} by reference (typing), saving memory.}
}
\value{
An \code{scr_split} object with \code{data} (typed), \code{target}, \code{train_idx},
\code{holdout_idx}, \code{method}, \code{cutoff}, \code{date_col} and \code{cols} (\code{features},
\code{var_num}, \code{var_cat}, \code{dropped}, \code{event}).
}
\description{
First stage of the pipeline, callable on its own. Converts the target to
0/1 (resolving \code{event_level}), types the candidates (numerics become
\code{double}, everything else becomes \code{character}) and splits train and
hold-out \strong{before} any supervised fit.
}
\details{
The split prefers out-of-time by \code{date_col}: it is the only one that
tests generalisation to a future period. The cut is made on the
\strong{distinct} date values, not by row quantile: it picks the smallest set
of most recent periods that already reaches \code{ratio} of the population.
Without a date column, or with a single period, it falls back to random
stratified by the target. The date column is never a candidate: it is the
key of the split and leaves the contest.
}
\section{Event orientation}{


\code{event_level} changes \strong{what is modelled}. Passing \code{0} makes class 0 the
event: the sign of every WOE flips, the emitted SQL changes, the points
change. For a text target, the second level in alphabetical order is the
event by default, and the choice is always reported. Not to be confused
with \code{config$objective}, which only orients the reading and the points
scale.
}

\examples{
sp <- scr_split(scr_demo, "default", date_col = "ref_date", drop = "id")
sp
length(sp$train_idx); length(sp$holdout_idx)
}
\seealso{
Other stages:
\code{\link[=scr_align]{scr_align()}},
\code{\link[=scr_bin]{scr_bin()}},
\code{\link[=scr_cutoff]{scr_cutoff()}},
\code{\link[=scr_model]{scr_model()}},
\code{\link[=scr_reject]{scr_reject()}},
\code{\link[=scr_scorecard]{scr_scorecard()}},
\code{\link[=scr_select]{scr_select()}},
\code{\link[=scr_strategy]{scr_strategy()}},
\code{\link[=scr_triage]{scr_triage()}}
}
\concept{stages}
