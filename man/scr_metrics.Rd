% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{scr_metrics}
\alias{scr_metrics}
\title{AUC, KS and Gini of a score, with a bootstrap confidence interval}
\usage{
scr_metrics(
  score,
  y,
  higher_is_event = TRUE,
  ci = TRUE,
  n_boot = 200L,
  level = 0.95,
  seed = NULL,
  nthread = 1L
)
}
\arguments{
\item{score}{Numeric vector with the score.}

\item{y}{0/1 outcome vector, same length as \code{score}.}

\item{higher_is_event}{If \code{TRUE} (default), a higher score means a higher
probability of the event (logit, probability, propensity score). Pass
\code{FALSE} for a credit points score (\code{higher_is_safer}), and the AUC is
reported above 0.5 when the score ranks correctly.}

\item{ci}{Compute the confidence interval. \code{FALSE} returns point estimates only.}

\item{n_boot}{Number of bootstrap resamples.}

\item{level}{Confidence level.}

\item{seed}{Bootstrap seed; \code{NULL} leaves it unset.}

\item{nthread}{Parallel workers for the resamples.}
}
\value{
A list of class \code{scr_metrics} with \code{auc}, \code{ks}, \code{gini}, the
bounds \code{auc_lo}/\code{auc_hi}, \code{ks_lo}/\code{ks_hi}, \code{gini_lo}/\code{gini_hi} (\code{NA}
when \code{ci = FALSE}), \code{n}, \code{events}, \code{n_boot} and \code{level}. Everything is
\code{NA_real_} when only one class is present or no valid case exists.
}
\description{
AUC through the Mann-Whitney U statistic with tie correction, computed on
the table of counts per unique score: a WOE score is constant within the
bin, so ties are the rule. Everything in \code{double} on purpose: with integer
counts, \code{n1 * n0} overflows \code{2^31} from about 46 thousand observations per
class and returns \code{NA}.
}
\details{
The confidence interval is \strong{always} computed by default (project
decision D18): a bootstrap stratified by outcome, percentile method, with
\code{n_boot} resamples. Gini is derived from AUC (\code{2 * AUC - 1}) inside each
resample, never bootstrapped separately. The cost is absorbed by
\code{nthread} (parallelism by resample).
}
\examples{
set.seed(1)
y <- rep(0:1, each = 500)
s <- stats::rnorm(1000) + 0.8 * y
m <- scr_metrics(s, y, n_boot = 50, seed = 1)
m
as.data.frame(m)
}
\references{
DeLong, E. R., DeLong, D. M. and Clarke-Pearson, D. L. (1988). Comparing
the areas under two or more correlated receiver operating characteristic
curves. \emph{Biometrics}, 44(3), 837-845.
}
\seealso{
Other metrics:
\code{\link[=scr_iv]{scr_iv()}},
\code{\link[=scr_psi]{scr_psi()}}
}
\concept{metrics}
