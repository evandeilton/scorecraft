% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{scr_psi}
\alias{scr_psi}
\title{Population stability index, with the fixed and the sample-size-adjusted threshold}
\usage{
scr_psi(
  base,
  compare,
  levels = NULL,
  breaks = NULL,
  n_groups = 10L,
  alpha = 0.05,
  thresholds = c(0.1, 0.25)
)
}
\arguments{
\item{base}{Reference vector (the "development" distribution).}

\item{compare}{Vector to compare.}

\item{levels}{For categorical vectors: the levels to consider. \code{NULL}
uses the union of the observed ones.}

\item{breaks}{For numeric vectors: frozen cut points. \code{NULL} derives
\code{n_groups} quantiles of \code{base}.}

\item{n_groups}{Number of bands when \code{breaks = NULL}.}

\item{alpha}{Significance level of the adjusted threshold.}

\item{thresholds}{The two fixed thresholds: below the first the flag is
\code{"stable"}, below the second \code{"moderate"}, otherwise \code{"shift"}.}
}
\value{
A list of class \code{scr_psi} with \code{psi}, \code{flag_fixed}, \code{critical}
(adjusted critical value), \code{flag_adjusted} (\code{"stable"} or \code{"shift"}),
\code{n_base}, \code{n_compare}, \code{n_bins} and \code{table} (per band: \code{pct_base},
\code{pct_compare}, \code{psi_band}).
The \code{thresholds} and \code{alpha} used are stored and printed.
}
\description{
\code{PSI = sum((p - q) * ln(p / q))} over bins frozen on the base. Reports
both thresholds side by side: the traditional fixed one (\verb{< 0.10}
\code{"stable"}, \code{0.10-0.25} \code{"moderate"}, \verb{>= 0.25} \code{"shift"}) and the
sample-size-adjusted critical value of Yurdakul and Naranjo (2020), under
which the PSI is asymptotically \code{(1/n + 1/m) * chi-squared(B - 1)}. With
\code{n = m = 1000} and ten bins the 5\% critical value is 0.034, not 0.10; on a
monthly base of a hundred thousand rows, \code{PSI = 0.01} is already
significant. The fixed threshold remains what the market knows; the
adjusted one is what the statistics support.
}
\examples{
set.seed(2)
base <- stats::rnorm(5000)
new  <- stats::rnorm(5000, mean = 0.15)
p <- scr_psi(base, new)
p
p$table
}
\references{
Yurdakul, B. and Naranjo, J. (2020). Statistical properties of the
population stability index. \emph{Journal of Risk Model Validation}, 14(4),
89-100.
}
\seealso{
Other metrics:
\code{\link[=scr_iv]{scr_iv()}},
\code{\link[=scr_metrics]{scr_metrics()}}
}
\concept{metrics}
