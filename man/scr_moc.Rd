% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd.R
\name{scr_moc}
\alias{scr_moc}
\title{Margin of conservatism, by category}
\usage{
scr_moc(
  x,
  category = c("A", "B", "C"),
  method = NULL,
  level = NULL,
  value = NULL,
  reason = NULL,
  dr = NULL,
  n_boot = 200L,
  seed = NULL
)
}
\arguments{
\item{x}{An \code{\link[=scr_grades]{scr_grades()}} object.}

\item{category}{\code{"A"}, \code{"B"} or \code{"C"}.}

\item{method}{For \code{"C"}: \code{"ci_timeseries"}, \code{"ci_binomial"} or
\code{"bootstrap"}; \code{NULL} reads \code{config$pd_moc_method}.}

\item{level}{One-sided confidence level; \code{NULL} reads \code{config$pd_moc_level}.}

\item{value}{For \code{"A"}/\code{"B"}: the add-on in PD units, length 1 or one per grade.}

\item{reason}{Justification (mandatory for \code{"A"}/\code{"B"}).}

\item{dr}{Optional \code{scr_dr} by grade for \code{"ci_timeseries"}, keyed by the
final grades of \code{x}; \code{NULL} uses the series already stored in \code{x$dr} by
\code{\link[=scr_grades]{scr_grades()}}.}

\item{n_boot, seed}{Bootstrap resamples and seed for \code{"bootstrap"}.}
}
\value{
The \code{scr_grades} object with the entries appended to \code{moc}.
}
\description{
Appends entries to the MoC ledger of an \code{\link[=scr_grades]{scr_grades()}} object. Category
\code{"C"} (general estimation error) is quantified: \code{"ci_timeseries"} takes
the upper bound of a one-sided \code{level} interval of the long-run average
from the cohort series, \eqn{t_{q, T-1}\, sd(DR_t)/\sqrt{T}} per grade;
\code{"ci_binomial"} uses \eqn{z_q \sqrt{PD(1-PD)/n}} on the obligors (or
obligor-years when a series exists); \code{"bootstrap"} resamples the
outcomes of the sample within each grade and takes the \code{level} quantile
of the default rate above the estimate. Categories \code{"A"} (data and
methodological deficiencies) and \code{"B"} (changes in standards or
environment) are expert quantities: \code{value} (one number or one per
grade, in PD units) and a non-empty \code{reason} are mandatory. The ledger
is append-only: \code{A} and \code{B} entries accumulate, a new \code{C} supersedes the
previous one (kept with \code{active = FALSE}).
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  use_lightgbm = FALSE, xgb_rounds = 40, n_boot = 10)
res <- scr_select(scr_demo, "default", config = cfg, drop = c("id", "churn"),
                  date_col = "ref_date")
sc <- scr_scorecard(res)
gr <- scr_grades(sc, n_grades = 6, min_defaults = 10)
gr <- scr_moc(gr, "C", method = "ci_binomial")
gr <- scr_moc(gr, "A", value = 0.002, reason = "missing unlikeliness-to-pay trigger before 2024")
gr$moc
}
\seealso{
Other irb-pd:
\code{\link[=predict.scr_grades]{predict.scr_grades()}},
\code{\link[=predict.scr_pd]{predict.scr_pd()}},
\code{\link[=scr_calibrate]{scr_calibrate()}},
\code{\link[=scr_grades]{scr_grades()}},
\code{\link[=scr_master_scale]{scr_master_scale()}},
\code{\link[=scr_migration]{scr_migration()}},
\code{\link[=scr_pd]{scr_pd()}},
\code{\link[=scr_pd_pit_ttc]{scr_pd_pit_ttc()}},
\code{\link[=scr_pd_validate]{scr_pd_validate()}}
}
\concept{irb-pd}
