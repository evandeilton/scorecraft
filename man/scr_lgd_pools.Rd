% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lgd.R
\name{scr_lgd_pools}
\alias{scr_lgd_pools}
\title{LGD pools from the predicted LGD}
\usage{
scr_lgd_pools(x, n_pools = NULL, min_defaults = NULL)
}
\arguments{
\item{x}{An \code{\link[=scr_lgd]{scr_lgd()}} object.}

\item{n_pools}{Target number of pools; \code{NULL} uses \code{lgd_n_pools}.}

\item{min_defaults}{Minimum defaults per pool; \code{NULL} uses
\code{lgd_min_defaults_bin}.}
}
\value{
A \code{data.table} with one row per pool: \code{pool}, \code{pred_lo},
\code{pred_hi}, \code{pred_mean}, \code{n}, \code{share}, \code{ead}, \code{lra}, \code{lra_ew}, \code{sd},
\code{se}, \code{moc_c}, \code{lra_moc}, \code{merged_from}.
}
\description{
Cuts the training predictions into \code{n_pools} quantile bands, merges the
bands with fewer than \code{min_defaults} defaults into the neighbour with
the closer long-run average, then merges adjacent bands whose long-run
averages break the increasing order (pool-adjacent violators), so that
the pools are ordered both in predicted and in realised LGD. Per pool:
the default-weighted long-run average (the regulatory estimate), the
exposure-weighted one, the standard error, the category-C margin of
conservatism (one-sided 95\% t interval on the mean) and their sum.
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, n_boot = 20)
wo <- scr_workout(scr_demo_lgd, scr_demo_lgd_cashflows, rates = scr_demo_rates, config = cfg)
m <- scr_lgd(wo, drivers = c("product", "ltv", "prior_dpd_max"), config = cfg)
scr_lgd_pools(m, n_pools = 4)
}
\seealso{
Other irb-lgd:
\code{\link[=scr_elbe]{scr_elbe()}},
\code{\link[=scr_lgd]{scr_lgd()}},
\code{\link[=scr_lgd_downturn]{scr_lgd_downturn()}},
\code{\link[=scr_lgd_floor]{scr_lgd_floor()}},
\code{\link[=scr_lgd_validate]{scr_lgd_validate()}},
\code{\link[=scr_workout]{scr_workout()}}
}
\concept{irb-lgd}
