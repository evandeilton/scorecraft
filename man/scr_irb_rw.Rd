% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capital.R
\name{scr_irb_rw}
\alias{scr_irb_rw}
\title{IRB risk weight of one or many exposures}
\usage{
scr_irb_rw(
  pd,
  lgd,
  ead = 1,
  m = NULL,
  asset_class,
  sales = NULL,
  fi = FALSE,
  defaulted = NULL,
  elbe = NULL,
  params = scr_irb_params("bcb"),
  approach = c("airb", "firb"),
  apply_floors = TRUE,
  collateral = NULL,
  secured_share = NULL
)
}
\arguments{
\item{pd, lgd, ead}{Numeric vectors: probability of default, loss given
default (decimals) and exposure at default (currency).}

\item{m}{Effective maturity in years (wholesale classes only; \code{NULL} or
\code{NA} uses \code{params$m_default}).}

\item{asset_class}{One of \code{"corporate"}, \code{"corporate_sme"}, \code{"bank"},
\code{"sovereign"}, \code{"hvcre"}, \code{"retail_mortgage"}, \code{"qrre_revolver"},
\code{"qrre_transactor"}, \code{"retail_other"}; a scalar or a vector.}

\item{sales}{Annual sales of \code{corporate_sme} obligors, in the unit of
\code{params$correlation$sme} (missing values take the lower bound, the
largest adjustment).}

\item{fi}{Logical: regulated financial institution above the size
threshold, or unregulated one (correlation multiplier).}

\item{defaulted}{Optional 0/1 or logical vector.}

\item{elbe}{Optional vector with the best estimate of expected loss of
the defaulted rows (decimal of \code{ead}); ignored on performing rows.}

\item{params}{An \code{\link[=scr_irb_params]{scr_irb_params()}} object.}

\item{approach}{\code{"airb"} (own LGD, floored) or \code{"firb"} (supervisory LGD
supplied by the caller, no LGD floor, maturity fixed).}

\item{apply_floors}{\code{TRUE} (all input floors), \code{FALSE} (none) or a subset
of \code{c("pd", "lgd", "m")}.}

\item{collateral}{Optional column of \code{params$lgd_floor} naming the
collateral type of each exposure (\code{"financial"}, \code{"receivables"},
\code{"real_estate"}, \code{"other_physical"}); \code{NULL} means unsecured.}

\item{secured_share}{Optional secured share in \verb{[0, 1]} blending the
unsecured and the collateral floors.}
}
\value{
A \code{data.table} with one row per exposure: \code{pd_used}, \code{lgd_used}
(after floors; PD one on defaulted rows), \code{m} (after clipping), \code{r},
\code{b}, \code{ma}, \code{k}, \code{rw}, \code{rwa}; attribute \code{floors_hit} counts the rows
where each floor was binding.
}
\description{
The asymptotic single risk factor function, vectorised over exposures:
PD floors by asset class, LGD input floors for own estimates
(\code{approach = "airb"}; the unsecured column of \code{params$lgd_floor} unless
\code{collateral} names another column, blended with \code{secured_share}), the
asset correlation of the class (with the firm-size adjustment of
\code{corporate_sme} from \code{sales} and the multiplier for large or unregulated
financial institutions when \code{fi} is \code{TRUE}), the maturity adjustment for
wholesale classes only (\code{m} clipped to \code{params$m_range}, \code{params$m_default}
when missing or under the foundation approach) and
}
\details{
\deqn{K = \left[LGD \cdot N\left(\frac{G(PD) + \sqrt{R}\,G(0.999)}{\sqrt{1-R}}\right) - PD \cdot LGD\right] \cdot MA \cdot s}

with \code{s = params$scaling_factor}. Defaulted rows carry \code{K = max(0, LGD - ELBE)} under \code{"airb"} and zero under \code{"firb"}; a missing \code{elbe} is taken
equal to \code{lgd}. \verb{RW = 12.5 K} and \code{RWA = RW * ead}.
}
\examples{
scr_irb_rw(0.01, 0.45, m = 2.5, asset_class = "corporate")
scr_irb_rw(c(0.01, 0.02), c(0.20, 0.80), asset_class = c("retail_mortgage", "qrre_revolver"))
r <- scr_irb_rw(1e-4, 0.5, asset_class = "retail_other")
attr(r, "floors_hit")
}
\references{
Basel Committee on Banking Supervision (2023). \emph{The Basel Framework},
CRE31 (IRB approach: risk-weight functions) and CRE32 (risk components).
BCBS (2005). \emph{An explanatory note on the Basel II IRB risk weight
functions}.
}
\seealso{
Other irb-capital:
\code{\link[=scr_capital]{scr_capital()}},
\code{\link[=scr_ecl]{scr_ecl()}},
\code{\link[=scr_el]{scr_el()}},
\code{\link[=scr_pd_stress]{scr_pd_stress()}},
\code{\link[=scr_sa_rw]{scr_sa_rw()}}
}
\concept{irb-capital}
