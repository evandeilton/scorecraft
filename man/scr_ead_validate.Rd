% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ead.R
\name{scr_ead_validate}
\alias{scr_ead_validate}
\title{Validate CCF pools: calibration, discrimination, back-testing and stability}
\usage{
scr_ead_validate(
  x,
  newdata = NULL,
  lights = c(0.01, 0.05),
  adequacy_lights = c(1, 1.05)
)
}
\arguments{
\item{x}{An \code{\link[=scr_ead]{scr_ead()}} object.}

\item{newdata}{\code{NULL} (the hold-out rows of \code{x}), an \code{\link[=scr_ead_data]{scr_ead_data()}}
object or its \code{rds} table.}

\item{lights}{Two increasing p-value thresholds: red at or below the
first, amber at or below the second.}

\item{adequacy_lights}{Two increasing adequacy-ratio thresholds.}
}
\value{
An object of class \code{scr_ead_validation}: \code{calibration},
\code{discrimination}, \code{backtest}, \code{stability}, \code{summary} (test,
statistic, p, light), \code{n}, \code{source}.
}
\description{
Per pool and in total, compares realised and predicted values on the
validation rows (the hold-out of the model by default): simple and
exposure-weighted averages, the one-sided t-test of realised above
predicted (under-estimation) with its p-value, the EAD adequacy ratio
(sum of realised EAD over sum of predicted EAD) and traffic lights
(red at or below \code{lights[1]}, amber at or below \code{lights[2]}, green above;
adequacy green at or below \code{adequacy_lights[1]}, amber up to
\code{adequacy_lights[2]}, red above). Adds the
discrimination block (gAUC with a bootstrap interval against the
development value, Spearman correlation, cumulative EAD accuracy
ratio), the back-test by cohort and the stability of the pool
distribution and of the driver bins (\code{\link[=scr_psi]{scr_psi()}}, fixed and
sample-size-adjusted thresholds). The numeric limits of the lights are
a convention of the package, stated as such in the output.
}
\examples{
cfg <- scr_config(verbose = FALSE, n_boot = 20, nthread = 1)
ed <- scr_ead_data(scr_demo_ead, facility_id = "facility_id", date_col = "ref_date",
                   limit = "limit", drawn = "drawn", defaulted = "defaulted",
                   drivers = c("product", "months_on_book"), config = cfg)
m <- scr_ead(ed, drivers = c("utilisation_ref", "product"), config = cfg)
v <- scr_ead_validate(m)
v
v$calibration
}
\seealso{
Other irb-ead:
\code{\link[=scr_bin_continuous]{scr_bin_continuous()}},
\code{\link[=scr_ead]{scr_ead()}},
\code{\link[=scr_ead_data]{scr_ead_data()}},
\code{\link[=scr_ead_downturn]{scr_ead_downturn()}}
}
\concept{irb-ead}
