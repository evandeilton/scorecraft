% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monitor.R
\name{scr_monitor}
\alias{scr_monitor}
\title{Monitor the scorecard on new data}
\usage{
scr_monitor(
  x,
  newdata,
  date_col = NULL,
  target = NULL,
  alpha = NULL,
  n_boot = NULL,
  plan = NULL
)
}
\arguments{
\item{x}{An object from \code{\link[=scr_scorecard]{scr_scorecard()}}.}

\item{newdata}{New table with the source columns.}

\item{date_col}{Period column. \code{NULL} treats \code{newdata} as a single period.}

\item{target}{Target column in \code{newdata}, for the performance by vintage.
\code{NULL} skips it.}

\item{alpha}{Level of the adjusted threshold. \code{NULL} (default) takes it
from the plan.}

\item{n_boot}{CI resamples per vintage. \code{NULL} uses the configuration.}

\item{plan}{The monitoring contract: \code{NULL} (default) uses the plan stored
in the scorecard (\code{\link[=scr_monitoring_plan]{scr_monitoring_plan()}}); otherwise an \code{item}/\code{value}
table, or the path of a strategy workbook written by \code{\link[=scr_export]{scr_export()}},
whose \code{Monitoring_Plan} sheet is read. The fixed thresholds of the PSI
and CSI flags, the alpha of the adjusted threshold and
\code{min_events_per_period} come from it.}
}
\value{
An \code{scr_monitor} object with \code{psi} (score, per period), \code{csi}
(per variable and period), \code{vintage} (or \code{NULL}; \code{status} says
\code{"insufficient"} when a period has fewer events than the plan requires)
and \code{plan} (the contract actually used).
}
\description{
Recomputes, per period of \code{date_col} (or for the whole data), the score
PSI against train with frozen bands, the CSI of every variable with
frozen bins plus the signed points shift and, when the target is
present, the performance by vintage (event rate, AUC/KS/Gini with CI).
Always reports both thresholds (fixed and n-adjusted). Schedules
nothing: the analyst calls it when needed.
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
res <- scr_select(scr_demo, "default", config = cfg, drop = "id",
                  date_col = "ref_date")
sc <- scr_scorecard(res)
mo <- scr_monitor(sc, scr_demo, date_col = "ref_date", target = "default")
mo
mo$psi
head(mo$csi)
}
\seealso{
Other production:
\code{\link[=predict.scr_align]{predict.scr_align()}},
\code{\link[=scr_apply]{scr_apply()}},
\code{\link[=scr_export]{scr_export()}},
\code{\link[=scr_monitoring_plan]{scr_monitoring_plan()}},
\code{\link[=scr_reasons]{scr_reasons()}},
\code{\link[=scr_sql]{scr_sql()}}
}
\concept{production}
