% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align.R
\name{scr_align}
\alias{scr_align}
\title{Stage 5: align a raw score to the declared scale}
\usage{
scr_align(
  raw,
  y,
  base_score = 600,
  base_odds = 50,
  pdo = 20,
  direction = c("higher_is_safer", "higher_is_riskier"),
  method = c("regression", "direct"),
  n_bands = 10L,
  laplace = 0.5,
  weights = NULL
)
}
\arguments{
\item{raw}{Raw score: an event logit (or any score on which a higher
value means a higher probability of the event).}

\item{y}{0/1 outcome vector, same length as \code{raw}.}

\item{base_score}{Score at which the odds are \code{base_odds}.}

\item{base_odds}{Odds at \code{base_score}, positive, in the orientation of \code{direction}.}

\item{pdo}{Points that double the odds, positive.}

\item{direction}{\code{"higher_is_safer"} or \code{"higher_is_riskier"}.}

\item{method}{\code{"regression"} (default) or \code{"direct"}.}

\item{n_bands}{Bands of the calibration regression.}

\item{laplace}{Smoothing of the counts per band.}

\item{weights}{Optional weights per observation (sample reweighting).}
}
\value{
An \code{scr_align} object with \code{base_score}, \code{base_odds}, \code{pdo},
\code{direction}, \code{odds_orientation}, \code{factor}, \code{offset}, \code{sign},
\code{calibration} (\code{method}, \code{intercept}, \code{slope}, \code{r2}, \code{n_bands}, \code{bands})
and the final coefficients \code{a} and \code{b} of \code{score = a + b * raw}. Use
\code{\link[=predict.scr_align]{predict.scr_align()}} to apply it.
}
\description{
Takes the raw score of \strong{any} engine (the scorecard logit, the output of
a tree challenger, a legacy score) to the scale defined by \code{base_score},
\code{base_odds} and \code{pdo}, recording \code{odds_orientation} on the object. This is
what makes two scorecards directly comparable. It runs automatically
inside \code{\link[=scr_scorecard]{scr_scorecard()}} (decision D9); it is exposed to align other
scores to the same scale.
}
\section{Mechanism}{


With \code{method = "regression"} (default): the raw score is banded by
quantiles on the reference data, the empirical log-odds of every band is
computed with Laplace smoothing in the orientation \code{direction} implies,
and a regression weighted by band size fits
\deqn{\ln(\mathrm{odds}) = I + S \cdot \mathrm{raw}.}
This absorbs sample reweighting, miscalibration of the WOE fit and prior
shift. It then composes with the PDO map:
\deqn{\mathrm{factor} = \mathrm{pdo}/\ln 2,\quad
      \mathrm{offset} = \mathrm{base\_score} - \mathrm{factor}\cdot\ln(\mathrm{base\_odds}),}
\deqn{\mathrm{score} = \mathrm{offset} + \mathrm{factor}\,(I + S\cdot\mathrm{raw}) = a + b\cdot\mathrm{raw}.}

With \code{method = "direct"}, the model is assumed calibrated: \code{I = 0} and
\code{S} is the sign of the direction (\code{-1} under \code{higher_is_safer}, \code{+1}
under \code{higher_is_riskier}), that is, \code{ln(odds)} is \code{raw} itself in the
right orientation.
}

\section{Odds orientation}{


\code{base_odds} is always expressed in the orientation \code{direction} implies:
non-event:event (\code{"safe:event"}) under \code{higher_is_safer}, event:non-event
(\code{"event:safe"}) under \code{higher_is_riskier}. The same word, \code{odds},
changes meaning between the two, and that is the most common sign trap in
the literature; hence the object records \code{odds_orientation} explicitly.
}

\examples{
set.seed(3)
y   <- stats::rbinom(4000, 1, 0.15)
raw <- stats::qlogis(0.15) + 1.3 * y + stats::rnorm(4000, sd = 1.2)
al  <- scr_align(raw, y, base_score = 600, base_odds = 50, pdo = 20)
al
head(predict(al, raw))
head(predict(al, raw, type = "prob"))

# propensity: more points = more event, odds event:non-event
scr_align(raw, y, base_score = 500, base_odds = 1/9, pdo = 40,
          direction = "higher_is_riskier")
}
\references{
Siddiqi, N. (2006). \emph{Credit Risk Scorecards}. Wiley, chapter 6.
}
\seealso{
Other stages:
\code{\link[=predict.scr_align]{predict.scr_align()}},
\code{\link[=scr_bin]{scr_bin()}},
\code{\link[=scr_cutoff]{scr_cutoff()}},
\code{\link[=scr_model]{scr_model()}},
\code{\link[=scr_monitor]{scr_monitor()}},
\code{\link[=scr_monitoring_plan]{scr_monitoring_plan()}},
\code{\link[=scr_reject]{scr_reject()}},
\code{\link[=scr_run]{scr_run()}},
\code{\link[=scr_scorecard]{scr_scorecard()}},
\code{\link[=scr_select]{scr_select()}},
\code{\link[=scr_split]{scr_split()}},
\code{\link[=scr_strategy]{scr_strategy()}},
\code{\link[=scr_triage]{scr_triage()}}
}
\concept{stages}
