% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classing.R
\name{scr_classing_propose}
\alias{scr_classing_propose}
\title{Propose manual bins for a variable}
\usage{
scr_classing_propose(
  lab,
  variable,
  breaks = NULL,
  groups = NULL,
  merge = NULL,
  split = NULL,
  missing_to = NULL,
  other_to = NULL,
  reset = FALSE
)
}
\arguments{
\item{lab}{An object from \code{\link[=scr_coarse_classing]{scr_coarse_classing()}}.}

\item{variable}{A variable of the lab.}

\item{breaks}{Numeric: interior cut points, \verb{(-Inf, b1], (b1, b2], ...}.}

\item{groups}{Categorical: a list of character vectors, one per bin;
names are display labels. Every training category must be assigned,
or \code{other_to} must name the catch-all bin.}

\item{merge}{Bin ids to merge (adjacent for numerics).}

\item{split}{\code{c(id, at)}: split numeric bin \code{id} at \code{at}.}

\item{missing_to}{Categorical: fold the \code{"MISSING"} category into this bin.}

\item{other_to}{Categorical: the bin that receives every training
category not listed in \code{groups}.}

\item{reset}{\code{TRUE} proposes a return to the optimal bins.}
}
\value{
An \code{scr_classing_proposal} with \code{id}, \code{variable}, \code{instruction}
(the resolved instruction as text), \code{entry} (the hand-built bins),
\code{checks}, \code{optimal} (the checks of the optimal bins), \code{compare},
\code{verdict} (\code{ACCEPTABLE}, \code{REVIEW} or \code{BLOCKED}), \code{warnings} and
\code{blocking}.
}
\description{
Exactly one of \code{breaks}, \code{groups}, \code{merge}, \code{split} or \code{reset} per call;
\code{missing_to} and \code{other_to} compose with a categorical instruction. The
instruction is resolved against the current bins into an absolute
specification, the WOE is refitted on the training rows, the bins are
applied frozen to the hold-out, and the comparison against the optimal
bins is printed. The proposal is a value: nothing changes in the lab
until \code{\link[=scr_classing_accept]{scr_classing_accept()}}.
}
\seealso{
\code{\link[=scr_coarse_classing]{scr_coarse_classing()}} for a complete session, from lab to
scorecard.

Other classing:
\code{\link[=scr_classing_accept]{scr_classing_accept()}},
\code{\link[=scr_classing_apply]{scr_classing_apply()}},
\code{\link[=scr_classing_choose]{scr_classing_choose()}},
\code{\link[=scr_classing_spec]{scr_classing_spec()}},
\code{\link[=scr_classing_view]{scr_classing_view()}},
\code{\link[=scr_coarse_classing]{scr_coarse_classing()}},
\code{\link[=scr_decisions]{scr_decisions()}}
}
\concept{classing}
