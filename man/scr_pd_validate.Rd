% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd.R
\name{scr_pd_validate}
\alias{scr_pd_validate}
\title{Validate a PD model on a cohort panel}
\usage{
scr_pd_validate(
  x,
  newdata,
  id = "id",
  date = "date",
  default = "default",
  grade = NULL,
  score = NULL,
  auc_init = NULL,
  cv_init = NULL,
  tests = c("jeffreys", "binomial", "normal", "hl", "multi_period", "auc",
    "concentration", "psi", "migration"),
  alpha = 0.05,
  lights = NULL,
  pd_column = c("pd_final", "pd_moc", "pd_be"),
  horizon = 12L,
  by = NULL,
  n_boot = NULL,
  seed = NULL
)
}
\arguments{
\item{x}{An \code{\link[=scr_pd]{scr_pd()}} object.}

\item{newdata}{A \code{data.frame}/\code{data.table} panel, one row per \code{id} and month.}

\item{id, date, default}{Column names.}

\item{grade}{Column name of the grade at every month; \code{NULL} derives it
from \code{score} with the cut points of \code{x}.}

\item{score}{Column name of the production score at every month, optional.}

\item{auc_init}{Development AUC; \code{NULL} uses the scorecard's hold-out AUC.}

\item{cv_init}{Development coefficient of variation; \code{NULL} uses the one of \code{x}.}

\item{tests}{Subset of the battery to run.}

\item{alpha}{Significance level of the binomial critical count.}

\item{lights}{Two p-value thresholds (red at or below the first, amber at
or below the second, green above; the convention shared with the LGD
and EAD validations); \code{NULL} reads \code{config$pd_lights}.}

\item{pd_column}{Grade PD tested: \code{"pd_final"} (default), \code{"pd_moc"} or \code{"pd_be"}.}

\item{horizon, by}{Cohort window in months and frequency (\code{NULL} reads \code{config$pd_dr_by}).}

\item{n_boot, seed}{Bootstrap resamples and seed of the discrimination interval.}
}
\value{
An object of class \code{scr_pd_validation}: \code{calibration} (per
grade, pooled), \code{calibration_cohort} (per cohort and grade),
\code{portfolio} (per cohort), \code{portfolio_tests} (list: \code{n}, \code{d}, \code{dr},
\code{pd}, \code{p_jeffreys}, \code{p_binomial}, \code{hl_chi2}, \code{hl_df}, \code{hl_p},
\code{multi_period_z}, \code{multi_period_p}, \code{brier}), \code{discrimination},
\code{stability} (\code{psi} table, \code{migration}, \code{concentration}), \code{summary}
(one row per test with \code{statistic}, \code{p_value}, \code{light}), \code{light}
(the worst light of the summary), \code{n_cohorts}, \code{alpha}, \code{lights}.
\code{portfolio_tests} also carries \code{critical}, \code{z}, \code{p_normal}, \code{n_cohorts}
and \code{pd_column}; the object also has \code{horizon}, \code{by}, \code{pd_column} and
\code{target}.
}
\description{
Runs the standard battery on a monthly panel with the default flag and
the grade (or the score) at every month: obligors non-defaulted at each
cohort start form the population, the outcome is a default within
\code{horizon} months, exactly as \code{\link[=scr_default_rate]{scr_default_rate()}} does.
}
\details{
\describe{
\item{Calibration}{Per grade (pooled over cohorts) and per cohort and
grade: Jeffreys \verb{p = F_Beta(PD; D + 1/2, N - D + 1/2)}, the binomial
\code{P(X >= D)} with its critical count at \code{alpha}, the normal \code{z}, and
the traffic light on the Jeffreys p-value. Portfolio: the same tests
on the totals, Hosmer-Lemeshow over the grades (\code{K - 2} degrees of
freedom), the multi-period normal test over the cohort default rates
and the Brier score.}
\item{Discrimination}{AUC, Gini and KS with a bootstrap interval
(\code{\link[=scr_metrics]{scr_metrics()}}) on the score when a \code{score} column exists,
otherwise on the grade; the \code{S} statistic against \code{auc_init}
(\code{(AUC_init - AUC_curr) / se}, Hanley-McNeil), \code{p = 1 - Phi(S)}.}
\item{Stability}{PSI of the grade distribution against the development
sample per cohort (\code{\link[=scr_psi]{scr_psi()}}); the migration matrix pooled over
the cohorts whose end date is observed (\code{\link[=scr_migration]{scr_migration()}}); the
concentration test on the coefficient of variation of the latest
cohort against \code{cv_init}.}
}
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  use_lightgbm = FALSE, xgb_rounds = 40, n_boot = 10)
res <- scr_select(scr_demo, "default", config = cfg, drop = c("id", "churn"),
                  date_col = "ref_date")
sc <- scr_scorecard(res)
pd <- scr_pd(scr_moc(scr_grades(sc, n_grades = 6, min_defaults = 10), "C", method = "ci_binomial"))
# the validation panel: default flag at every month plus the grade at the
# cohort start; here the behavioural score of the panel is graded with the
# cut points of the PD model
d <- scr_default(scr_demo_panel, "id", "ref_date", dpd = "dpd", config = cfg)
pnl <- merge(d$flags, scr_demo_panel[, c("id", "ref_date", "score")],
             by.x = c("id", "date"), by.y = c("id", "ref_date"))
pnl$grade <- predict(pd, score = pnl$score, type = "grade")
v <- scr_pd_validate(pd, pnl, id = "id", date = "date", default = "default",
                     grade = "grade", score = "score", by = "quarter")
v
v$summary
}
\seealso{
Other irb-pd:
\code{\link[=predict.scr_grades]{predict.scr_grades()}},
\code{\link[=predict.scr_pd]{predict.scr_pd()}},
\code{\link[=scr_calibrate]{scr_calibrate()}},
\code{\link[=scr_grades]{scr_grades()}},
\code{\link[=scr_master_scale]{scr_master_scale()}},
\code{\link[=scr_migration]{scr_migration()}},
\code{\link[=scr_moc]{scr_moc()}},
\code{\link[=scr_pd]{scr_pd()}},
\code{\link[=scr_pd_pit_ttc]{scr_pd_pit_ttc()}}
}
\concept{irb-pd}
