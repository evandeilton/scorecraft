% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd.R
\name{scr_pd}
\alias{scr_pd}
\title{The PD model: grades, margin of conservatism and the floor}
\usage{
scr_pd(
  grades,
  moc = NULL,
  params = NULL,
  asset_class = NULL,
  philosophy = c("ttc", "pit"),
  rho = NULL,
  z = NULL
)
}
\arguments{
\item{grades}{An \code{\link[=scr_grades]{scr_grades()}} object, after \code{\link[=scr_moc]{scr_moc()}}.}

\item{moc}{\code{NULL} uses \code{grades$moc}; otherwise a ledger in the same format.}

\item{params}{An \code{\link[=scr_irb_params]{scr_irb_params()}}; \code{NULL} uses \code{config$framework}.}

\item{asset_class}{Asset class of the floor; \code{NULL} uses \code{config$pd_asset_class}.}

\item{philosophy}{\code{"ttc"} (default) or \code{"pit"}.}

\item{rho, z}{Asset correlation and systematic factor for \code{"pit"}.}
}
\value{
An object of class \code{scr_pd}: \code{table} (\code{grade}, \code{label},
\code{score_lo}, \code{score_hi}, \code{n}, \code{share}, \code{defaults}, \code{dr}, \code{pd_be},
\code{moc_a}, \code{moc_b}, \code{moc_c}, \code{pd_moc}, \code{pd_ttc}, \code{pd_pit}, \code{floor},
\code{pd_final}, \code{floor_applied}), \code{breaks}, \code{band_grade}, \code{direction},
\code{alignment}, \code{alignment_score}, \code{master_scale}, \code{asset_class},
\code{framework}, \code{floor}, \code{philosophy}, \code{rho}, \code{z}, \code{moc_ledger},
\code{calibration}, \code{concentration}, \code{portfolio} (weighted \code{pd_be},
\code{pd_moc}, \code{pd_final}, \code{moc_bp}, \code{share_at_floor}), \code{scorecard},
\code{ledger}, \code{model_card}.
}
\description{
Assembles the final grade table: \code{pd_be} from \code{\link[=scr_grades]{scr_grades()}}, the active
entries of the MoC ledger by category (\code{A} and \code{B} summed over their
entries, the latest \code{C}), \code{pd_moc = pd_be + A + B + C}, the PD floor of
the asset class under the framework of \code{params}, and
\code{pd_final = max(pd_moc, floor)}. With \code{philosophy = "pit"} the
through-the-cycle \code{pd_moc} is converted with the one-factor bridge of
\code{\link[=scr_pd_pit_ttc]{scr_pd_pit_ttc()}} before the floor (\code{rho} and \code{z} required).
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  use_lightgbm = FALSE, xgb_rounds = 40, n_boot = 10)
res <- scr_select(scr_demo, "default", config = cfg, drop = c("id", "churn"),
                  date_col = "ref_date")
sc <- scr_scorecard(res)
gr <- scr_moc(scr_grades(sc, n_grades = 6, min_defaults = 10), "C", method = "ci_binomial")
pd <- scr_pd(gr)
pd
pd$table[, c("grade", "pd_be", "moc_c", "pd_final", "floor_applied")]
head(predict(pd, score = c(480, 560, 640), type = "pd_final"))
head(scr_apply(pd, head(scr_demo, 5)))
cat(tail(scr_sql(pd), 8), sep = "\n")
}
\seealso{
Other irb-pd:
\code{\link[=predict.scr_grades]{predict.scr_grades()}},
\code{\link[=predict.scr_pd]{predict.scr_pd()}},
\code{\link[=scr_calibrate]{scr_calibrate()}},
\code{\link[=scr_grades]{scr_grades()}},
\code{\link[=scr_master_scale]{scr_master_scale()}},
\code{\link[=scr_migration]{scr_migration()}},
\code{\link[=scr_moc]{scr_moc()}},
\code{\link[=scr_pd_pit_ttc]{scr_pd_pit_ttc()}},
\code{\link[=scr_pd_validate]{scr_pd_validate()}}
}
\concept{irb-pd}
