% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ead.R
\name{scr_ead_downturn}
\alias{scr_ead_downturn}
\title{Downturn CCF per pool}
\usage{
scr_ead_downturn(
  x,
  periods = NULL,
  method = NULL,
  add_on = 0.15,
  reason = NULL
)
}
\arguments{
\item{x}{An \code{\link[=scr_ead]{scr_ead()}} object.}

\item{periods}{A \code{data.frame} with \code{start} and \code{end} dates of the
downturn periods (needed for \code{"type1"}).}

\item{method}{\code{"type1"}, \code{"type3"} or \code{"none"}; \code{NULL} uses
\code{config$ccf_downturn}.}

\item{add_on}{Add-on of the \code{"type3"} method, in CCF units.}

\item{reason}{Text justifying the periods and the method; mandatory.}
}
\value{
The \code{scr_ead} object with \code{downturn} (a list with \code{method},
\code{periods}, \code{add_on} and the per-pool \code{table}: \code{pool}, \code{lra},
\code{n_downturn}, \code{dt_observed}, \code{dt_type3}, \code{ccf_dt}), the updated
\code{pools} and a new ledger row.
}
\description{
Quantifies the downturn component of the CCF from user-supplied downturn
periods. \code{"type1"} (observed impact) takes, per pool, the default-weighted
average of the realised values of the events whose default date falls in
the periods and sets \code{ccf_dt = max(lra, observed)}; \code{"type3"} (reference
value plus add-on) sets \code{ccf_dt = lra + add_on}; \code{"none"} resets
\code{ccf_dt = lra}. The pool table is recomputed (\code{ccf_final}, \code{ccf_applied})
and the ledger records the periods, the method and the reason.
}
\examples{
cfg <- scr_config(verbose = FALSE, n_boot = 20, nthread = 1)
ed <- scr_ead_data(scr_demo_ead, facility_id = "facility_id", date_col = "ref_date",
                   limit = "limit", drawn = "drawn", defaulted = "defaulted",
                   drivers = c("product", "months_on_book"), config = cfg)
m <- scr_ead(ed, drivers = c("utilisation_ref", "product"), config = cfg)
m2 <- scr_ead_downturn(m, periods = data.frame(start = as.Date("2024-01-01"),
                                               end = as.Date("2024-12-01")),
                       reason = "2024 chosen as the stress year of the demo panel")
m2$downturn$table
}
\seealso{
Other irb-ead:
\code{\link[=scr_bin_continuous]{scr_bin_continuous()}},
\code{\link[=scr_ead]{scr_ead()}},
\code{\link[=scr_ead_data]{scr_ead_data()}},
\code{\link[=scr_ead_validate]{scr_ead_validate()}}
}
\concept{irb-ead}
