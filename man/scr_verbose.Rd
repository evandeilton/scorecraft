% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{scr_verbose}
\alias{scr_verbose}
\title{Switch progress messages on or off}
\usage{
scr_verbose(on = NULL)
}
\arguments{
\item{on}{\code{TRUE} to switch on, \code{FALSE} to switch off, \code{NULL} (default) to
query the current state only.}
}
\value{
The verbosity state in force \emph{before} the call, invisibly, so that
\verb{old <- scr_verbose(FALSE); ...; scr_verbose(old)} restores it.
}
\description{
Large tables take tens of minutes, and the pipeline reports every stage as
it runs: stage name, input and output counts, elapsed time. Messages go
through \code{\link[=message]{message()}} as single lines, with no progress bar that redraws
itself, so that a scheduled job (\code{Rscript} in batch) produces a readable
log. \code{\link[=suppressMessages]{suppressMessages()}} works too; this function exists to switch them
off persistently, without wrapping every call. The \code{verbose} key of
\code{\link[=scr_config]{scr_config()}} has the same effect per run.
}
\examples{
old <- scr_verbose(FALSE)   # silence, keeping the previous state
scr_verbose(old)            # restore
scr_verbose()               # query
}
\seealso{
Other configuration:
\code{\link[=scr_config]{scr_config()}},
\code{\link[=scr_config_keys]{scr_config_keys()}},
\code{\link[=scr_presets]{scr_presets()}}
}
\concept{configuration}
