% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capital.R
\name{scr_capital}
\alias{scr_capital}
\title{Expected loss, risk-weighted assets and capital of a portfolio}
\usage{
scr_capital(
  x,
  pd = "pd",
  lgd = "lgd",
  ead = "ead",
  segment = NULL,
  asset_class = config$capital_asset_class,
  m = NULL,
  defaulted = NULL,
  elbe = NULL,
  provisions = NULL,
  ltv = NULL,
  rating = NULL,
  sales = NULL,
  fi = NULL,
  transactor = NULL,
  grade = NULL,
  id = NULL,
  params = scr_irb_params(config$framework),
  config = scr_config(),
  keep_rows = FALSE
)
}
\arguments{
\item{x}{A table of exposures (\code{data.frame} or \code{data.table}) or the list
form described above.}

\item{pd, lgd, ead}{Column names of the probability of default, loss given
default and exposure at default.}

\item{segment}{Optional column name of the reporting segment.}

\item{asset_class}{A column name or a single asset class (see
\code{\link[=scr_irb_rw]{scr_irb_rw()}}).}

\item{m, defaulted, elbe, provisions, ltv, rating, sales, fi, transactor, grade, id}{Optional column names: effective maturity, default flag, best estimate
of expected loss, provision stock, loan-to-value, external rating,
annual sales, financial-institution flag, transactor flag, PD grade
(defines the SQL pools together with \code{segment}) and exposure
identifier.}

\item{params}{An \code{\link[=scr_irb_params]{scr_irb_params()}} object; defaults to the preset of
\code{config$framework}.}

\item{config}{An \code{\link[=scr_config]{scr_config()}} object (\code{capital_approach},
\code{capital_target_ratio}, \code{capital_output_floor}, \code{capital_sensitivity},
\code{nthread}, \code{verbose}).}

\item{keep_rows}{Keep the per-exposure table in the object.}
}
\value{
An object of class \code{scr_capital}: a list with \code{exposures}
(per-exposure table, only with \code{keep_rows = TRUE}), \code{segments} (the
reconciliation table: \code{segment}, \code{n}, \code{ead}, \code{pd_mean}, \code{lgd_mean},
\code{m}, \code{r_mean}, \code{k_mean}, \code{rw}, \code{rwa_irb}, \code{rwa_sa}, \code{irb_sa_ratio},
\code{el}, \code{provisions}, \code{shortfall_excess}), \code{pools} (one row per
segment and grade with the constants the SQL emits), \code{totals} (\code{n},
\code{ead}, \code{el}, \code{rwa_irb}, \code{rwa_sa}, \code{irb_sa_ratio}, \code{output_floor},
\code{rwa_floor}, \code{rwa_reported}, \code{floor_binding}, \code{headroom}, \code{density},
\code{target_ratio}, \code{capital}, \code{provisions}, \code{shortfall}, \code{excess},
\code{tier2_addback}, \code{tier2_cap}, \code{hhi}, \code{n_eff}, \code{max_share},
\code{granular}), \code{floors} (\code{floor}, \code{n_hit}, \code{ead_hit}, \code{delta_rwa}),
\code{sensitivity} (\code{shock}, \code{rwa}, \code{delta}, \code{delta_pct}),
\code{concentration} (share of EAD and RWA by segment), \code{framework},
\code{approach}, \code{params}, \code{config}, \code{ledger}, \code{model_card} and, after
\code{\link[=scr_export]{scr_export()}}, \code{files}.
}
\description{
Runs \code{\link[=scr_irb_rw]{scr_irb_rw()}} on every exposure, aggregates by segment, compares
the IRB result with the standardised approach for the output floor,
reconciles regulatory expected loss with the provision stock
(shortfall deducted from capital; excess eligible as tier 2 up to 0.6 \%
of the IRB risk-weighted assets), measures the impact of each input
floor, runs a fixed sensitivity grid and reports the name concentration
of the book. The parameter tables come from \code{params}; the object records
whether they were edited.
}
\section{Inputs}{


\code{x} is either a table of exposures, the remaining arguments naming its
columns, or a list \code{list(pd = , lgd = , ead = , data = )} whose elements
are fitted models with an \code{\link[=scr_apply]{scr_apply()}} method (the PD, LGD and EAD
objects of the IRB modules) and \code{data} the table to apply them to. In
the list form each model present fills the corresponding vector from the
columns \code{pd_final}, \code{lgd_final} and \code{ead_predicted} of its \code{scr_apply()}
output, and the provenance is written to the ledger; elements that are
\code{NULL} fall back to the named columns of \code{data}.

\code{asset_class} is a column name of \code{x} or a single class applied to every
row. Segment means are weighted by EAD. The sensitivity grid shocks the
PD (x1.10, x1.25, x1.50), the LGD (+5 percentage points), the EAD
(+10 \%), removes the input floors, scales the correlation (x1.25) and
stresses the PD with the one-factor model at \code{q = 0.95} and \code{0.99}
(\code{\link[=scr_pd_stress]{scr_pd_stress()}}, the stressed PD then re-entering the function so
that the correlation follows it).
}

\examples{
cfg <- scr_config(verbose = FALSE)
cap <- scr_capital(scr_demo_portfolio, segment = "segment", asset_class = "asset_class",
                   m = "m", defaulted = "defaulted", elbe = "elbe", provisions = "provision",
                   ltv = "ltv", rating = "rating", sales = "sales", transactor = "transactor",
                   grade = "grade", id = "id", config = cfg)
cap
cap$segments[, c("segment", "n", "rw", "irb_sa_ratio")]
cap$floors
}
\references{
Basel Committee on Banking Supervision (2023). \emph{The Basel Framework},
CRE31, CRE35 (treatment of expected losses and provisions), RBC20
(output floor).
}
\seealso{
Other irb-capital:
\code{\link[=scr_ecl]{scr_ecl()}},
\code{\link[=scr_el]{scr_el()}},
\code{\link[=scr_irb_rw]{scr_irb_rw()}},
\code{\link[=scr_pd_stress]{scr_pd_stress()}},
\code{\link[=scr_sa_rw]{scr_sa_rw()}}
}
\concept{irb-capital}
