% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply.R
\name{scr_reasons}
\alias{scr_reasons}
\title{Reason codes: the variables that took the most points from each row}
\usage{
scr_reasons(x, newdata, k = 4L, reference = c("mean", "max"))
}
\arguments{
\item{x}{An object from \code{\link[=scr_scorecard]{scr_scorecard()}}.}

\item{newdata}{New table.}

\item{k}{Number of reasons per row.}

\item{reference}{\code{"mean"} (default) or \code{"max"}.}
}
\value{
A \code{data.table} with \code{reason_1} ... \code{reason_k} (variable names)
and \code{shortfall_1} ... \code{shortfall_k} (points below the reference).
}
\description{
For each row of \code{newdata}, the \code{k} variables whose contribution in points
fell furthest below the reference. The reference is the mean points of
the variable on the training population (\code{"mean"}, the Regulation B safe
harbour referenced to the average) or the maximum points of the variable
(\code{"max"}). Only applies to the additive scorecard; a tree challenger has
no reason codes.
}
\details{
Under \code{higher_is_riskier} the shortfall is measured the other way round:
the reasons are the variables that added the most points.
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
res <- scr_select(scr_demo, "default", config = cfg, drop = "id",
                  date_col = "ref_date")
sc <- scr_scorecard(res)
scr_reasons(sc, head(scr_demo, 5), k = 3)
}
\references{
12 CFR 1002.9 (Regulation B), official commentary to paragraph 9(b)(2).
}
\seealso{
Other production:
\code{\link[=predict.scr_align]{predict.scr_align()}},
\code{\link[=scr_apply]{scr_apply()}},
\code{\link[=scr_export]{scr_export()}},
\code{\link[=scr_monitor]{scr_monitor()}},
\code{\link[=scr_monitoring_plan]{scr_monitoring_plan()}},
\code{\link[=scr_sql]{scr_sql()}}
}
\concept{production}
