% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply.R
\name{scr_apply}
\alias{scr_apply}
\alias{scr_apply.scr_result}
\alias{scr_apply.scr_scorecard}
\title{Apply the WOE transformation or the scorecard to new data}
\usage{
scr_apply(x, newdata, ...)

\method{scr_apply}{scr_result}(
  x,
  newdata,
  features = scr_selected(x),
  what = c("woe", "bin", "both"),
  ...
)

\method{scr_apply}{scr_scorecard}(x, newdata, what = c("score", "points", "woe", "all"), ...)
}
\arguments{
\item{x}{An object from \code{\link[=scr_select]{scr_select()}} (returns WOE/bin of the approved
variables) or from \code{\link[=scr_scorecard]{scr_scorecard()}} (returns score and points).}

\item{newdata}{New table with the source columns of the requested
variables. The target column is not needed.}

\item{...}{Passed on to the methods.}

\item{features}{For \code{scr_result}: which variables to transform. Defaults to
the approved ones.}

\item{what}{For \code{scr_result}: \code{"woe"}, \code{"bin"} or \code{"both"}. For
\code{scr_scorecard}: \code{"score"}, \code{"points"}, \code{"woe"} or \code{"all"}.}
}
\value{
A \code{data.table} with one row per row of \code{newdata}.
}
\description{
Materialises in R exactly what the production SQL does: the frozen Stage
1 pre-processing (training median, special-population flags,
\code{"MISSING"}) followed by the frozen Stage 2 binning and, for a scorecard,
by the points. Nothing is refitted. The two paths, R and SQL, produce the
same numbers, and a test guarantees it.
}
\section{Method arguments}{


For \code{scr_result}: \code{features} (default: the approved ones) and \code{what}
(\code{"woe"}, \code{"bin"} or \code{"both"}). For \code{scr_scorecard}: \code{what} (\code{"score"},
\code{"points"}, \code{"woe"} or \code{"all"}). The score output carries \code{link}
(logit), \code{prob} (model probability), \code{score} (exact, \code{a + b * logit}) and
\code{score_points} (the sum of the whole points per bin plus the base).
}

\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
res <- scr_select(scr_demo, "default", config = cfg, drop = "id",
                  date_col = "ref_date")
new <- head(scr_demo, 50)
str(scr_apply(res, new)[, 1:3])
sc <- scr_scorecard(res)
head(scr_apply(sc, new))
head(scr_apply(sc, new, what = "points"))
}
\seealso{
Other production:
\code{\link[=scr_export]{scr_export()}},
\code{\link[=scr_reasons]{scr_reasons()}},
\code{\link[=scr_sql]{scr_sql()}}
}
\concept{production}
