% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{scr_fetch}
\alias{scr_fetch}
\title{Fetch a table with reproducible server-side sampling}
\usage{
scr_fetch(
  con,
  table,
  sample_frac = 1,
  seed = NULL,
  max_rows = NULL,
  sample_expr = NULL
)
}
\arguments{
\item{con}{A DBI connection, from \code{\link[=scr_connect]{scr_connect()}}.}

\item{table}{Qualified table name.}

\item{sample_frac}{Fraction of rows to fetch, in (0, 1].}

\item{seed}{Seed of the server-side random function, where supported.}

\item{max_rows}{Row cap. \code{NULL} switches it off.}

\item{sample_expr}{Optional SQL expression yielding a uniform number in
\verb{[0, 1)}, used as \verb{WHERE <sample_expr> <= sample_frac}.}
}
\value{
A \code{data.table} with the fetched table.
}
\description{
Sampling happens on the server, not in R: pulling a million rows to
discard ninety per cent of them pays the network cost twice. \code{max_rows} is
a memory guard: when it binds, the requested fraction is reduced on the
server and the reduction is reported. The random expression follows the
connection class (\code{rand(seed)} on Spark/Databricks/MySQL, \code{random()} on
PostgreSQL/DuckDB, an integer-modulo expression on SQLite, which has no
seedable \code{random()}); pass \code{sample_expr} to override it.
}
\examples{
\dontshow{if (requireNamespace("RSQLite", quietly = TRUE)) withAutoprint(\{ # examplesIf}
con <- scr_connect(driver = RSQLite::SQLite(), dbname = ":memory:")
d <- scr_demo; d$ref_date <- as.character(d$ref_date)
DBI::dbWriteTable(con, "dtm", d)
nrow(scr_fetch(con, "dtm", sample_frac = 0.5, seed = 42))
nrow(scr_fetch(con, "dtm", max_rows = 1000))
DBI::dbDisconnect(con)
\dontshow{\}) # examplesIf}
}
\seealso{
Other database:
\code{\link[=scr_connect]{scr_connect()}}
}
\concept{database}
