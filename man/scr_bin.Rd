% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bin.R
\name{scr_bin}
\alias{scr_bin}
\title{Stage 2: optimal binning, screening, hold-out revalidation and pruning}
\usage{
scr_bin(triage, config = scr_config())
}
\arguments{
\item{triage}{An object from \code{\link[=scr_triage]{scr_triage()}}.}

\item{config}{An object from \code{\link[=scr_config]{scr_config()}}.}
}
\value{
An \code{scr_bins} object with \code{fit} (an \code{obwoe} object), \code{screen}
(\code{summary} and \code{full}), \code{holdout}, \code{prune}, \code{pool} (eligible for the
models), \code{derived_excluded}, the counts \code{binned}, \code{pos_screen} and
\code{pos_holdout} (the survivors of each gate, in order), the WOE matrices
\code{woe_train}/\code{woe_holdout}, the originating \code{triage} and the \code{config}.
}
\description{
Fits the bins \strong{on the training rows only} (cut points and WOE use the
target), in parallel by column, and applies in sequence:
}
\details{
\enumerate{
\item \strong{Screening}, native to the engine: eight admission rules
(\code{IV_BELOW_MIN}, \code{IV_SUSPICIOUS}, \code{NOT_MONOTONIC}, \code{TOO_FEW_BINS},
\code{TOO_MANY_BINS}, \code{SMALL_BIN}, \code{DEGENERATE_BIN}, \code{BINNING_ERROR}).
\item \strong{Hold-out revalidation} with frozen bins: IV recomputed on the
same labels, train/hold-out PSI (the fixed threshold decides; the
n-adjusted one is reported) and the fraction of hold-out without a bin.
\item \strong{Redundancy pruning} by rank correlation on the WOE space,
ranked by hold-out IV.
}
}
\section{Parallelism}{


Columns are split into \code{config$nthread} chunks, each chunk is binned by a
worker and the fits are merged. The result is identical to the serial one
(a test pins this): the engine is deterministic per column.
}

\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1)
sp <- scr_split(scr_demo, "default", date_col = "ref_date", drop = "id")
bn <- scr_bin(scr_triage(sp, cfg), cfg)
bn
head(bn$holdout)
}
\seealso{
Other stages:
\code{\link[=scr_align]{scr_align()}},
\code{\link[=scr_cutoff]{scr_cutoff()}},
\code{\link[=scr_model]{scr_model()}},
\code{\link[=scr_reject]{scr_reject()}},
\code{\link[=scr_scorecard]{scr_scorecard()}},
\code{\link[=scr_select]{scr_select()}},
\code{\link[=scr_split]{scr_split()}},
\code{\link[=scr_strategy]{scr_strategy()}},
\code{\link[=scr_triage]{scr_triage()}}
}
\concept{stages}
