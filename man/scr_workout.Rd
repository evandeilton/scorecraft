% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lgd.R
\name{scr_workout}
\alias{scr_workout}
\title{Workout LGD: the reference data set from default events and cash flows}
\usage{
scr_workout(
  defaults,
  cashflows,
  rates = NULL,
  config = scr_config(),
  indirect_costs = 0,
  obs_date = NULL,
  keep_rows = FALSE
)
}
\arguments{
\item{defaults}{A \code{data.frame} or \code{data.table} with one row per default
event: \code{default_id}, \code{facility_id}, \code{default_date}, \code{ead}, \code{product},
\code{status} (\code{"closed"}, \code{"cured"} or \code{"open"}), optionally \code{close_date},
plus any driver columns, which are carried into the RDS.}

\item{cashflows}{Long table: \code{default_id}, \code{date}, \code{amount}, \code{type}
(\code{"recovery"}, \code{"direct_cost"} or \code{"drawing"}).}

\item{rates}{Optional table \verb{(date, rate)} of the annual reference rate;
the rate in force at the default date is used. \code{NULL} uses the flat
\code{lgd_discount_rate} of the configuration.}

\item{config}{A \code{\link[=scr_config]{scr_config()}}; keys \verb{lgd_*}.}

\item{indirect_costs}{Total indirect workout cost to allocate: a single
number, or a table \verb{(product, amount)} allocated within product.}

\item{obs_date}{Observation date; \code{NULL} uses the latest date seen.}

\item{keep_rows}{Keep the cash-flow table with its present values.}
}
\value{
An object of class \code{scr_workout}: \code{rds} (one row per default
event: identifiers, \code{default_date}, \code{ead}, \code{product}, drivers,
\code{status}, \code{months_in_default}, \code{discount_rate}, \code{pv_recovery},
\code{pv_cost}, \code{pv_drawing}, \code{cost_indirect}, \code{recovery_extrapolated},
\code{lgd_raw}, \code{lgd_real}, \code{is_cure}, \code{is_incomplete}, \code{merged_n}),
\code{recovery_profile} (product x month: \code{cum_recovery}), \code{extrapolation}
(per open event), \code{funnel} (rule, n, action), \code{summary} (\code{n},
\code{cure_rate}, \code{lra_default_weighted}, \code{lra_exposure_weighted},
\code{share_incomplete}, \code{discount_rate_mean}, \code{by_product}, \code{by_year}),
\code{ledger}, \code{config}, \code{obs_date}, and \code{cashflows} with \code{keep_rows}.
}
\description{
Builds the reference data set (RDS) of realised loss given default, one
row per default event, from a table of default events and the long table
of their post-default cash flows. Every cash flow is discounted to the
default date at the reference rate in force at that date plus
\code{lgd_discount_add_on}, with monthly compounding over whole months:
\deqn{\mathrm{PV} = \frac{A}{(1 + r/12)^{t}}}
where \code{t} is the number of whole months between the default date and the
cash-flow date. The realised LGD is the economic loss
\deqn{\mathrm{LGD} = \frac{E - \mathrm{PV}(R) + \mathrm{PV}(C) + \mathrm{PV}(D) + C^{\mathrm{ind}}}{E}}
with \code{E} the exposure at default, \code{R} recoveries, \code{C} direct costs, \code{D}
drawings after default and \code{C^ind} the indirect costs allocated by
\code{lgd_cost_allocation}.
}
\section{Rules}{

\itemize{
\item \strong{Cures.} An event with \code{status == "cured"} returns to performing: the
balance outstanding at the cure date (\code{ead} net of the cash recovered)
enters as an artificial recovery on the cure date, so the cure carries
its costs and the discount effect, never a zero loss by decree.
\item \strong{Multiple defaults.} Two defaults of one facility separated by fewer
than \code{lgd_cure_window} months (from the close of the first to the start
of the second), or a new default while the first is still open, are
one event: the first default date and exposure are kept, the cash
flows of both spells are pooled and the status of the last spell rules.
\item \strong{Incomplete workouts.} An open event younger than \code{lgd_t_max} months
receives the expected further recovery read from the recovery profile
of the closed defaults of the same product (cumulative discounted
recovery rate by month in default); an open event at or beyond
\code{lgd_t_max} is treated as closed with no further recovery.
\item \strong{Bounds.} With \code{lgd_floor_at_zero} the realised LGD used in the
averages is floored at zero and with \code{lgd_cap_at_one} capped at one;
\code{lgd_raw} always keeps the unbounded value.
}

The long-run average is reported default-weighted (the arithmetic mean
over events) and exposure-weighted, overall, by product and by calendar
year of default.
}

\examples{
cfg <- scr_config(verbose = FALSE)
wo <- scr_workout(scr_demo_lgd, scr_demo_lgd_cashflows, rates = scr_demo_rates, config = cfg)
wo
wo$funnel
head(wo$rds[, c("default_id", "product", "status", "lgd_raw", "lgd_real", "is_cure")])
}
\seealso{
Other irb-lgd:
\code{\link[=scr_elbe]{scr_elbe()}},
\code{\link[=scr_lgd]{scr_lgd()}},
\code{\link[=scr_lgd_downturn]{scr_lgd_downturn()}},
\code{\link[=scr_lgd_floor]{scr_lgd_floor()}},
\code{\link[=scr_lgd_pools]{scr_lgd_pools()}},
\code{\link[=scr_lgd_validate]{scr_lgd_validate()}}
}
\concept{irb-lgd}
