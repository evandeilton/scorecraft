% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cutoff.R
\name{scr_reject}
\alias{scr_reject}
\title{Stage 6: honest reject inference through a sensitivity band}
\usage{
scr_reject(
  x,
  population = NULL,
  accepted = NULL,
  multipliers = NULL,
  sample = "holdout"
)
}
\arguments{
\item{x}{An object from \code{\link[=scr_scorecard]{scr_scorecard()}}.}

\item{population}{Optional: a table of the full population (accepted and
rejected, without outcome), scored by \code{\link[=scr_apply]{scr_apply()}}. \code{NULL} restricts
the scope to the population with an outcome.}

\item{accepted}{Optional: a logical vector, of the length of
\code{population}, marking the rows with an observed outcome. \code{NULL} treats
the whole \code{population} as without an outcome beyond the development sample.}

\item{multipliers}{Sensitivity band. \code{NULL} uses the configuration.}

\item{sample}{Reference sample of the observed outcomes.}
}
\value{
An \code{scr_reject} object with \code{scope}, \code{coverage} (per band) and
\code{sensitivity} (per band and multiplier, plus the \code{TOTAL} row).
}
\description{
Does not ship parcelling as the default behaviour (decision D16): instead
of inventing a single multiplier and reweighting, it declares the
\strong{population scope} of the scorecard, measures the \strong{coverage per band}
(where an observed outcome exists, and in what volume) and presents a
\strong{sensitivity band}: the event rate each band would have if the
population without an outcome were 2, 4 or 8 times worse than the
observed one, with the effect on the total. The analyst reads the band;
no single number is fabricated.
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
res <- scr_select(scr_demo, "default", config = cfg, drop = "id",
                  date_col = "ref_date")
sc <- scr_scorecard(res)
scr_reject(sc)
# with a through-the-door population: rows with an outcome are the hold-out
acc <- seq_len(nrow(scr_demo)) \%in\% res$split$holdout_idx
scr_reject(sc, population = scr_demo, accepted = acc)
}
\seealso{
Other stages:
\code{\link[=predict.scr_align]{predict.scr_align()}},
\code{\link[=scr_align]{scr_align()}},
\code{\link[=scr_bin]{scr_bin()}},
\code{\link[=scr_cutoff]{scr_cutoff()}},
\code{\link[=scr_model]{scr_model()}},
\code{\link[=scr_monitor]{scr_monitor()}},
\code{\link[=scr_monitoring_plan]{scr_monitoring_plan()}},
\code{\link[=scr_run]{scr_run()}},
\code{\link[=scr_scorecard]{scr_scorecard()}},
\code{\link[=scr_select]{scr_select()}},
\code{\link[=scr_split]{scr_split()}},
\code{\link[=scr_strategy]{scr_strategy()}},
\code{\link[=scr_triage]{scr_triage()}}
}
\concept{stages}
