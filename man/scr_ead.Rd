% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ead.R
\name{scr_ead}
\alias{scr_ead}
\title{Estimate CCF pools from the reference data set}
\usage{
scr_ead(x, drivers, config = scr_config(), holdout = 0.3, params = NULL)
}
\arguments{
\item{x}{An \code{\link[=scr_ead_data]{scr_ead_data()}} object.}

\item{drivers}{Column names of the candidate drivers (columns of
\code{x$rds}).}

\item{config}{A \code{\link[=scr_config]{scr_config()}}.}

\item{holdout}{Hold-out share, by whole reference dates.}

\item{params}{An \code{\link[=scr_irb_params]{scr_irb_params()}} object; \code{NULL} uses the preset of
\code{config$framework}.}
}
\value{
An object of class \code{scr_ead}: \code{pools} (the pool table),
\code{cells} (every cell of the cross with its pool), \code{bins} (the
\code{obwoe}-shaped fit of the admitted drivers), \code{bins_all} (the fit of
every driver), \code{drivers} (admission table), \code{holdout} (frozen bins on
the hold-out), \code{rds} (the reference rows with \code{sample} and \code{pool}),
\code{metrics} (per sample: \code{rmse}, \code{mae}, \code{gauc} with a bootstrap interval,
\code{spearman}, \code{ead_rmse}, \code{ead_mae}, \code{adequacy}, \code{cear}), \code{split},
\code{funnel}, \code{data_summary}, \code{downturn}, \code{ledger}, \code{model_card},
\code{params}, \code{config}, \code{meta}.
}
\description{
Splits the reference data set by reference date (the most recent
cohorts form the hold-out), bins every driver against the realised CCF
with the continuous binner (\code{\link[=scr_bin_continuous]{scr_bin_continuous()}}) on the training
rows, revalidates the frozen bins on the hold-out and admits a driver
when it passes the named rules \code{TOO_FEW_DEFAULTS}, \code{NO_SEPARATION},
\code{NOT_MONOTONIC} and \code{UNSTABLE_HOLDOUT}. The cells of the cross of the
admitted drivers are ordered by their predicted CCF and merged, adjacent
cells first, down to \code{config$ccf_n_pools} pools with at least
\code{config$ccf_min_defaults} defaults each. Rows in the limit-factor
measure form their own pool \code{LF}.
}
\details{
Per pool the estimate is the long-run (default-weighted) average of the
realised values on the training rows, \code{lra}; \code{moc_est} is the one-sided
normal estimation-error margin at \code{config$ccf_moc_alpha};
\code{ccf_dt} is the downturn value (equal to \code{lra} until
\code{\link[=scr_ead_downturn]{scr_ead_downturn()}} is run); \code{ccf_final = max(lra, ccf_dt) + moc_est};
\code{ccf_floor = params$ccf_floor_fraction * config$ccf_sa_ccf}; and
\code{ccf_applied = max(ccf_final, ccf_floor)}. For the \code{LF} pool the floor
depends on the utilisation and is applied per row by \code{\link[=scr_apply]{scr_apply()}}.
}
\examples{
cfg <- scr_config(verbose = FALSE, n_boot = 20, nthread = 1)
ed <- scr_ead_data(scr_demo_ead, facility_id = "facility_id", date_col = "ref_date",
                   limit = "limit", drawn = "drawn", defaulted = "defaulted",
                   drivers = c("product", "months_on_book", "dpd"), config = cfg)
m <- scr_ead(ed, drivers = c("utilisation_ref", "product", "months_on_book"), config = cfg)
m
m$pools
m$drivers
}
\seealso{
Other irb-ead:
\code{\link[=scr_bin_continuous]{scr_bin_continuous()}},
\code{\link[=scr_ead_data]{scr_ead_data()}},
\code{\link[=scr_ead_downturn]{scr_ead_downturn()}},
\code{\link[=scr_ead_validate]{scr_ead_validate()}}
}
\concept{irb-ead}
