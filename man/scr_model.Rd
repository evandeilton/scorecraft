% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{scr_model}
\alias{scr_model}
\title{Stages 3 and 4: multi-strategy selection and consensus}
\usage{
scr_model(bins, config = scr_config())
}
\arguments{
\item{bins}{An object from \code{\link[=scr_bin]{scr_bin()}}.}

\item{config}{An object from \code{\link[=scr_config]{scr_config()}}.}
}
\value{
An \code{scr_models} object with \code{votes} (one row per model and
feature), \code{metrics} (one row per model, with CI), \code{consensus} (\code{table},
\code{selected}, \code{meta}) and the originating \code{bins}.
}
\description{
Trains the classifiers enabled in the configuration on the WOE columns of
the eligible pool, measures each on the hold-out (AUC/KS/Gini with a
bootstrap CI) and combines the votes:
}
\details{
\preformatted{
consensus_score = mean of the importance rank percentiles, weighted by the
                  hold-out Gini of each model
votes           = how many models elected the feature (top-K, or non-zero
                  coefficient in the elastic net)
}

The final cut respects \verb{[target_min, target_max]}. If the strict
consensus does not reach \code{target_min}, relaxation happens in \strong{named},
recorded steps (\code{min_votes} reduced; completed by score), never
resurrecting a feature failed by an earlier gate.
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
sp <- scr_split(scr_demo, "default", date_col = "ref_date", drop = "id")
md <- scr_model(scr_bin(scr_triage(sp, cfg), cfg), cfg)
md
md$consensus$selected
}
\seealso{
Other stages:
\code{\link[=scr_align]{scr_align()}},
\code{\link[=scr_bin]{scr_bin()}},
\code{\link[=scr_cutoff]{scr_cutoff()}},
\code{\link[=scr_reject]{scr_reject()}},
\code{\link[=scr_scorecard]{scr_scorecard()}},
\code{\link[=scr_select]{scr_select()}},
\code{\link[=scr_split]{scr_split()}},
\code{\link[=scr_strategy]{scr_strategy()}},
\code{\link[=scr_triage]{scr_triage()}}
}
\concept{stages}
