% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd.R
\name{scr_calibrate}
\alias{scr_calibrate}
\title{Calibrate the alignment to a central tendency}
\usage{
scr_calibrate(
  x,
  target,
  sample_rate = NULL,
  method = NULL,
  ar_target = NULL,
  segment = NULL,
  raw = NULL,
  y = NULL,
  sample = "holdout"
)
}
\arguments{
\item{x}{An \code{\link[=scr_scorecard]{scr_scorecard()}} (uses the ln(odds) and outcome of
\code{sample}), an \code{\link[=scr_align]{scr_align()}} (pass \code{raw} and, for the two-parameter
methods, \code{y}) or a numeric vector of event ln(odds) (aligned directly
to the default 600/50/20 scale).}

\item{target}{The central tendency: a number in \verb{(0, 1)} or an \code{scr_dr}
from \code{\link[=scr_default_rate]{scr_default_rate()}} (its \code{lra$mean} is used). With \code{segment}, a
named vector with one CT per segment.}

\item{sample_rate}{Event rate of the calibration sample; \code{NULL} uses the
mean of \code{y}.}

\item{method}{\code{"intercept"}, \code{"logodds_ab"}, \code{"scaling"} or \code{"qmm"};
\code{NULL} uses \code{config$pd_calibration}.}

\item{ar_target}{Target accuracy ratio for \code{"logodds_ab"} and \code{"qmm"}.}

\item{segment}{Optional vector of segment labels, one per calibration
row: one alignment per segment is fitted as well.}

\item{raw, y}{Raw ln(odds) and 0/1 outcome when \code{x} is not a scorecard.}

\item{sample}{Sample of the scorecard used for the calibration.}
}
\value{
An object of class \code{scr_pd_calibration}: \code{alignment} (the new
\code{scr_align}), \code{alignment_before}, \code{method}, \code{ct}, \code{target_source},
\code{sample_rate}, \code{shift} (change of the event intercept), \code{shift_prior}
(the closed-form King-Zeng shift), \code{slope_ratio} (\verb{S* / S}),
\code{mean_pd_before}, \code{mean_pd_after}, \code{ar_before}, \code{ar_after} (observed),
\code{ar_implied_before}, \code{ar_implied_after}, \code{n}, \code{segments} (table and
alignments when \code{segment} is given), \code{ledger}.
Also \code{ar_target}, \code{note} and \code{sample}.
}
\description{
Re-anchors the probability of default of a scorecard to a long-run
average default rate (the central tendency, CT) without touching the
points: the result is a \strong{new} alignment \verb{(I*, S*)} such that
\code{predict(alignment, raw, type = "prob")} is the calibrated PD, while the
scorecard keeps its own alignment for the score. Four methods:
}
\details{
\describe{
\item{\code{"intercept"}}{The prior-correction shift of King and Zeng
(2001), \eqn{\delta = \ln[\tau(1-\bar y) / ((1-\tau)\bar y)]}, added
to the event ln(odds); \code{S} unchanged, so the rank order and every
discrimination statistic are untouched. The closed form is exact on
the odds; when the calibration sample is available the shift is
refined by a one-dimensional root so that the mean PD equals the CT
exactly (the closed form is reported as \code{shift_prior}).}
\item{\code{"logodds_ab"}}{Tasche (2013): \verb{ln(odds*) = a + b ln(odds)}, with
\verb{(a, b)} solving \verb{mean(PD*) = CT} and implied accuracy ratio equal to
\code{ar_target} (default: the accuracy ratio observed on the sample).}
\item{\code{"qmm"}}{Quasi-moment matching: the same two equations, with the
target accuracy ratio taken from the PD distribution itself
(the implied AR of the current PDs), so no outcome is needed.}
\item{\code{"scaling"}}{\verb{PD* = PD * CT / ybar}. The proportional rescaling is
not a logit map, so the slope is the least-squares projection of
\verb{logit(PD*)} on the ln(odds) and the intercept is solved to the CT.}
}
}
\examples{
set.seed(1)
l <- stats::qlogis(0.12) + stats::rnorm(2000)
y <- stats::rbinom(2000, 1, stats::plogis(l))
cal <- scr_calibrate(l, target = 0.04, y = y)
cal
mean(predict(cal$alignment, l, type = "prob"))
scr_calibrate(l, target = 0.04, y = y, method = "logodds_ab", ar_target = 0.55)
}
\references{
King, G. and Zeng, L. (2001). Logistic regression in rare events data.
\emph{Political Analysis}, 9(2), 137-163.

Tasche, D. (2013). The art of probability-of-default curve calibration.
\emph{Journal of Credit Risk}, 9(4), 63-103.
}
\seealso{
Other irb-pd:
\code{\link[=predict.scr_grades]{predict.scr_grades()}},
\code{\link[=predict.scr_pd]{predict.scr_pd()}},
\code{\link[=scr_grades]{scr_grades()}},
\code{\link[=scr_master_scale]{scr_master_scale()}},
\code{\link[=scr_migration]{scr_migration()}},
\code{\link[=scr_moc]{scr_moc()}},
\code{\link[=scr_pd]{scr_pd()}},
\code{\link[=scr_pd_pit_ttc]{scr_pd_pit_ttc()}},
\code{\link[=scr_pd_validate]{scr_pd_validate()}}
}
\concept{irb-pd}
