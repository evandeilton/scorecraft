% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scorecard.R
\name{scr_score_gains}
\alias{scr_score_gains}
\title{Score gains per frozen band}
\usage{
scr_score_gains(x, sample = NULL)
}
\arguments{
\item{x}{An object from \code{\link[=scr_scorecard]{scr_scorecard()}}.}

\item{sample}{\code{NULL} (all), \code{"train"} or \code{"holdout"}.}
}
\value{
A \code{data.table} with one row per sample and band, from the riskiest
band to the safest.
}
\description{
How the score behaves in each band: count, event rate, KS, lift,
cumulative capture and the score interval of the band, which is what lets
a cut-off be read straight from the table. The bands are the deciles of
the score on \strong{train}, applied frozen to the other samples.
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
res <- scr_select(scr_demo, "default", config = cfg, drop = "id",
                  date_col = "ref_date")
sc <- scr_scorecard(res)
scr_score_gains(sc, "holdout")[, .(band, n, event_rate, min_score, max_score, ks)]
scr_score_metrics(sc)
}
\seealso{
Other accessors:
\code{\link[=scr_funnel]{scr_funnel()}},
\code{\link[=scr_gains]{scr_gains()}},
\code{\link[=scr_leakage]{scr_leakage()}},
\code{\link{scr_result}},
\code{\link[=scr_score_metrics]{scr_score_metrics()}},
\code{\link[=scr_selected]{scr_selected()}}
}
\concept{accessors}
