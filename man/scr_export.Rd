% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{scr_export}
\alias{scr_export}
\alias{scr_export.scr_result}
\alias{scr_export.scr_scorecard}
\title{Write the deliverables}
\usage{
scr_export(x, dir, stamp = TRUE, ...)

\method{scr_export}{scr_result}(x, dir, stamp = TRUE, ...)

\method{scr_export}{scr_scorecard}(x, dir, stamp = TRUE, ...)
}
\arguments{
\item{x}{An object from \code{\link[=scr_select]{scr_select()}} or \code{\link[=scr_scorecard]{scr_scorecard()}}.}

\item{dir}{Output directory. Created if it does not exist.}

\item{stamp}{If \code{TRUE} (default), writes to a timestamped subdirectory,
preserving earlier runs.}

\item{...}{For \code{scr_scorecard}: precomputed \code{cutoff}, \code{strategy},
\code{reject} and \code{monitor} objects, and \code{revenue_good}/\code{loss_bad} for the
default strategy table.}
}
\value{
The object \code{x}, with \verb{$files} filled, invisibly.
}
\description{
For an \code{scr_result}: the selection workbook (\verb{selection_<target>.xlsx}:
funnel, gains, screening, hold-out, models, votes, consensus, ledger,
redundancy), the WOE SQL and the executive summary in Markdown. For an
\code{scr_scorecard}: three workbooks (the detailing decision of SPEC section 7
resolved as separate files) plus the score SQL:
}
\details{
\describe{
\item{\verb{scorecard_<target>.xlsx}}{\code{Score_Summary} (with \code{odds_orientation}),
\code{Final_Scorecard}, \code{Coefficients}, \code{Sign_Check}, \code{Alignment},
\code{Model_Card}, \code{Challenger} and \code{Swap_Set} (when a challenger exists).}
\item{\verb{validation_<target>.xlsx}}{\code{Score_Gains_Frozen}, \code{Variable_Gains_IV},
\code{Discrimination_CI}, \code{Stability_PSI_Timeline}, \code{Stability_CSI_Timeline},
\code{Calibration}, \code{Performance_By_Vintage}, \code{Rank_Order_Diagnostics}.}
\item{\verb{strategy_<target>.xlsx}}{\code{Population_Scope}, \code{Cutoff_Sweep},
\code{Strategy_Bands}, \code{Reject_Sensitivity}, \code{Monitoring_Plan}.}
}

The timeline and vintage sheets need the date column of the split; when it
is absent they carry an availability row instead of a fabricated number.
}
\examples{
\dontshow{if (requireNamespace("openxlsx", quietly = TRUE)) withAutoprint(\{ # examplesIf}
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
res <- scr_select(scr_demo, "default", config = cfg, drop = "id",
                  date_col = "ref_date")
out <- file.path(tempdir(), "scorecraft-example")
res <- scr_export(res, out, stamp = FALSE)
basename(unlist(res$files))
sc <- scr_export(scr_scorecard(res), out, stamp = FALSE)
basename(unlist(sc$files))
\dontshow{\}) # examplesIf}
}
\seealso{
Other production:
\code{\link[=scr_apply]{scr_apply()}},
\code{\link[=scr_reasons]{scr_reasons()}},
\code{\link[=scr_sql]{scr_sql()}}
}
\concept{production}
