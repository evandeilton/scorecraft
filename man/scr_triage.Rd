% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triage.R
\name{scr_triage}
\alias{scr_triage}
\title{Stage 1: descriptive triage and sentinel resolution}
\usage{
scr_triage(split, config = scr_config())
}
\arguments{
\item{split}{An object from \code{\link[=scr_split]{scr_split()}}.}

\item{config}{An object from \code{\link[=scr_config]{scr_config()}}.}
}
\value{
An \code{scr_triage} object with \code{profile} (one row per candidate and
derived flag), \code{ledger} (the source of truth of the pre-processing the
SQL reproduces), \code{keep}, \code{derived}, \code{clean} (target + survivors + flags,
with no \code{NA}) and the originating \code{split}.
}
\description{
Profiles every candidate \strong{on the training rows only}, decides its fate
and materialises the clean data for train and hold-out with the same
values (training median, \code{"MISSING"} level). A sentinel or missing mass
with weight (\code{special_min_share}) and signal (\code{special_min_woe}) becomes a
categorical flag \verb{<column><flag_suffix>}, which the engine bins and emits
in SQL natively.
}
\details{
Failures at this stage: \code{CONSTANT}, \code{NEAR_CONSTANT}, \code{TOO_MANY_MISSING},
\code{HIGH_CARDINALITY}, \code{NO_SIGNAL} (coarse IV below \code{min_iv_quick}) and
\verb{DUPLICATE_OF:<column>}. A failed numeric whose special-population flag
survives carries the suffix \verb{;RESCUED_AS_FLAG}.
}
\examples{
sp <- scr_split(scr_demo, "default", date_col = "ref_date", drop = "id")
tr <- scr_triage(sp, scr_config(verbose = FALSE))
tr
table(tr$profile$triage_reason)
}
\seealso{
Other stages:
\code{\link[=scr_align]{scr_align()}},
\code{\link[=scr_bin]{scr_bin()}},
\code{\link[=scr_cutoff]{scr_cutoff()}},
\code{\link[=scr_model]{scr_model()}},
\code{\link[=scr_reject]{scr_reject()}},
\code{\link[=scr_scorecard]{scr_scorecard()}},
\code{\link[=scr_select]{scr_select()}},
\code{\link[=scr_split]{scr_split()}},
\code{\link[=scr_strategy]{scr_strategy()}}
}
\concept{stages}
