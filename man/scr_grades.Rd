% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd.R
\name{scr_grades}
\alias{scr_grades}
\title{Rating grades on the score}
\usage{
scr_grades(
  x,
  calibration = NULL,
  master_scale = NULL,
  n_grades = NULL,
  method = NULL,
  min_obligors = NULL,
  min_defaults = NULL,
  monotone = TRUE,
  pd_source = NULL,
  sample = "holdout",
  dr = NULL
)
}
\arguments{
\item{x}{An \code{\link[=scr_scorecard]{scr_scorecard()}}.}

\item{calibration}{An \code{\link[=scr_calibrate]{scr_calibrate()}} object (or its alignment);
\code{NULL} uses the scorecard's own alignment.}

\item{master_scale}{An \code{\link[=scr_master_scale]{scr_master_scale()}} for \code{method = "supplied"}
(optional for \code{"geometric"}).}

\item{n_grades, method, min_obligors, min_defaults, pd_source}{\code{NULL} reads
\code{pd_n_grades}, \code{pd_grade_method}, \code{pd_min_obligors}, \code{pd_min_defaults}
and \code{pd_source} from the scorecard configuration.}

\item{monotone}{Repair non-monotone grade PDs by pooling.}

\item{sample}{Sample of the scorecard used to build the grades.}

\item{dr}{Optional \code{scr_dr} with a \code{grade} column keyed by the final
grades (see the section above).}
}
\value{
An object of class \code{scr_grades}: \code{table} (\code{grade}, \code{label},
\code{score_lo}, \code{score_hi}, \code{pd_lo}, \code{pd_hi}, \code{n}, \code{share}, \code{defaults},
\code{dr}, \code{pd_mean}, \code{pd_be}, \code{merged_from}, and \code{n_series}, \code{t_series}
when a series is given), \code{breaks} (ascending score cut points),
\code{band_grade} (grade of every score band, ascending), \code{direction},
\code{method}, \code{pd_source}, \code{master_scale}, \code{alignment} (calibrated),
\code{alignment_score} (the scorecard's), \code{concentration} (\code{hhi}, \code{cv},
\code{hi}, \code{k}), \code{repairs}, \code{ledger}, \code{moc} (empty, filled by
\code{\link[=scr_moc]{scr_moc()}}), \code{dr} (the pooled series), \code{rows} (score, outcome and
grade of the sample), \code{scorecard}, \code{sample}, \code{ct}, \code{sample_rate}.
}
\description{
Cuts the production score into grades whose PD is monotone. The grade
boundaries are score cut points, direction-aware: grade 1 is the safest
(the highest scores under \code{higher_is_safer}). Three constructions:
\code{"geometric"} builds a \code{\link[=scr_master_scale]{scr_master_scale()}} between the 1st and 99th
percentiles of the calibrated PD and converts its PD bounds into scores
through the calibrated alignment; \code{"quantile"} cuts equal-count score
bands (cut points moved half-way between neighbouring scores, so a
boundary never sits on an observed value); \code{"supplied"} grades by the PD
bands of a given master scale.
}
\details{
Grades below \code{min_obligors} obligors or \code{min_defaults} defaults are
merged with the neighbour of closer default rate; the sequence of grade
PDs is then repaired by pool-adjacent-violators when \code{monotone = TRUE},
and every merge is recorded in \code{repairs}. The grade PD (\code{pd_be}) is the
long-run average of the grade default rates when a default-rate series
by grade is given in \code{dr} (\code{pd_source = "lra"}), the sample default rate
of the grade otherwise, or the mean of the calibrated individual PDs
(\code{pd_source = "mean_pd"}). Concentration is reported as the Herfindahl
index, the coefficient of variation of the grade shares and the
Herfindahl-based \code{hi} index.
}
\section{Two-pass workflow with a default-rate series}{


The series must be keyed by the final grades of this same call. Run
\code{\link[=scr_grades]{scr_grades()}} once, grade the cohort panel with \code{\link[=predict.scr_grades]{predict.scr_grades()}},
build the series with \code{\link[=scr_default_rate]{scr_default_rate()}} (\verb{grade =}) and pass it as
\code{dr} in a second call with identical arguments (or in \code{\link[=scr_moc]{scr_moc()}} and
\code{\link[=scr_pd_validate]{scr_pd_validate()}}, which read it the same way).
}

\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  use_lightgbm = FALSE, xgb_rounds = 40, n_boot = 10)
res <- scr_select(scr_demo, "default", config = cfg, drop = c("id", "churn"),
                  date_col = "ref_date")
sc <- scr_scorecard(res)
cal <- scr_calibrate(sc, target = 0.06)
gr <- scr_grades(sc, cal, n_grades = 7, min_defaults = 10)
gr
gr$table[, c("grade", "score_lo", "score_hi", "n", "dr", "pd_be")]
# grade a cohort panel with the score cut points
head(predict(gr, score = scr_demo_panel$score))
}
\seealso{
Other irb-pd:
\code{\link[=predict.scr_grades]{predict.scr_grades()}},
\code{\link[=predict.scr_pd]{predict.scr_pd()}},
\code{\link[=scr_calibrate]{scr_calibrate()}},
\code{\link[=scr_master_scale]{scr_master_scale()}},
\code{\link[=scr_migration]{scr_migration()}},
\code{\link[=scr_moc]{scr_moc()}},
\code{\link[=scr_pd]{scr_pd()}},
\code{\link[=scr_pd_pit_ttc]{scr_pd_pit_ttc()}},
\code{\link[=scr_pd_validate]{scr_pd_validate()}}
}
\concept{irb-pd}
