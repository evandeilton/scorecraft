% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd.R
\name{scr_migration}
\alias{scr_migration}
\title{Migration matrix between two rating dates}
\usage{
scr_migration(grade_t0, grade_t1, K = NULL)
}
\arguments{
\item{grade_t0, grade_t1}{Integer grades at the two dates, same length.}

\item{K}{Number of grades; \code{NULL} uses the largest grade observed.}
}
\value{
An object of class \code{scr_migration}: \code{matrix} (counts, \code{K} rows,
\code{K + 2} columns), \code{p} (row probabilities), \code{n} (row totals),
\code{mwb_upper}, \code{mwb_lower}, \code{z} (\verb{K x K}), \code{n_significant} (cells with
\code{z > 1.645}), \code{mobility} (\code{share_stable}, \code{share_up}, \code{share_down},
\code{mean_distance}, \code{share_default}, \code{share_closed}).
Also \code{K}, the number of grades.
}
\description{
Counts \code{N_ij} of obligors in grade \code{i} at the first date and grade \code{j}
at the second, the row probabilities \code{p_ij}, the upper and lower matrix
weighted bandwidths
\deqn{MWB_{up} = \frac{\sum_{i<j} |i-j|\, N_i\, p_{ij}}{\sum_i \max(|i-K|, |i-1|)\, N_i \sum_{j>i} p_{ij}},}
(and the mirror image for downgrades), the \code{z} statistic of every
off-diagonal cell against its neighbour closer to the diagonal (a
significantly positive value means the probability does not decay away
from the diagonal) and the mobility summary. Values of \code{grade_t1}
outside \verb{1..K} count as \code{default}, \code{NA} as \code{closed}; both stay out of
the bandwidths.
}
\examples{
set.seed(2)
g0 <- sample(1:5, 500, TRUE)
g1 <- pmin(5, pmax(1, g0 + sample(c(-1, 0, 0, 0, 1), 500, TRUE)))
g1[sample(500, 10)] <- NA
scr_migration(g0, g1, K = 5)
}
\seealso{
Other irb-pd:
\code{\link[=predict.scr_grades]{predict.scr_grades()}},
\code{\link[=predict.scr_pd]{predict.scr_pd()}},
\code{\link[=scr_calibrate]{scr_calibrate()}},
\code{\link[=scr_grades]{scr_grades()}},
\code{\link[=scr_master_scale]{scr_master_scale()}},
\code{\link[=scr_moc]{scr_moc()}},
\code{\link[=scr_pd]{scr_pd()}},
\code{\link[=scr_pd_pit_ttc]{scr_pd_pit_ttc()}},
\code{\link[=scr_pd_validate]{scr_pd_validate()}}
}
\concept{irb-pd}
