% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lgd.R
\name{scr_elbe}
\alias{scr_elbe}
\title{ELBE and in-default LGD on a grid of months since default}
\usage{
scr_elbe(x, grid = NULL)
}
\arguments{
\item{x}{An \code{\link[=scr_lgd]{scr_lgd()}} object.}

\item{grid}{Months since default; \code{NULL} uses \code{lgd_elbe_grid}.}
}
\value{
An object of class \code{scr_elbe}: \code{table} (\code{months_since_default},
\code{pool}, \code{n_open}, \code{share_open}, \code{recovered_share}, \code{elbe}, \code{delta_ul},
\code{lgd_in_default}), \code{consistency} (per pool at \code{tau = 0}), \code{grid}, \code{t_max}.
}
\description{
For every pool and every reference age \code{tau} of the grid, the expected
loss best estimate is the mean realised LGD of the training defaults of
the pool that were still in workout at \code{tau} (so that at \code{tau = 0} it
equals the pool's long-run average), and the in-default LGD adds the
unexpected-loss increment
\deqn{\Delta^{UL}(\tau) = (\mathrm{LGD}^{DT} - \mathrm{LRA})\;\frac{\rho(T_{\max}) - \rho(\tau)}{\rho(T_{\max})}}
read from the recovery profile of the pool's product mix: the downturn
uplift shrinks as the recoveries come in. The consistency table checks
that \code{lgd_in_default} at \code{tau = 0} reproduces the pool's \code{lgd_dt}.
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, n_boot = 20)
wo <- scr_workout(scr_demo_lgd, scr_demo_lgd_cashflows, rates = scr_demo_rates, config = cfg)
m <- scr_lgd(wo, drivers = c("product", "ltv", "prior_dpd_max"), config = cfg)
e <- scr_elbe(m)
e
}
\seealso{
Other irb-lgd:
\code{\link[=scr_lgd]{scr_lgd()}},
\code{\link[=scr_lgd_downturn]{scr_lgd_downturn()}},
\code{\link[=scr_lgd_floor]{scr_lgd_floor()}},
\code{\link[=scr_lgd_pools]{scr_lgd_pools()}},
\code{\link[=scr_lgd_validate]{scr_lgd_validate()}},
\code{\link[=scr_workout]{scr_workout()}}
}
\concept{irb-lgd}
