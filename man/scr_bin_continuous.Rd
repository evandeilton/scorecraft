% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irb-binning.R
\name{scr_bin_continuous}
\alias{scr_bin_continuous}
\title{Bin drivers against a continuous target (LGD, CCF)}
\usage{
scr_bin_continuous(
  data,
  target,
  features,
  train_idx = NULL,
  holdout_idx = NULL,
  min_bins = 2L,
  max_bins = 6L,
  min_share = 0.05,
  min_n = 30L,
  monotone = c("auto", "increasing", "decreasing", "none"),
  scale = c("mean", "logit"),
  nthread = 1L,
  alpha = 0.05
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table}.}

\item{target}{Column name of the continuous target.}

\item{features}{Column names of the drivers.}

\item{train_idx, holdout_idx}{Row indices; \code{NULL} uses every row for
training and skips the revalidation.}

\item{min_bins, max_bins}{Target range of bins per driver.}

\item{min_share}{Minimum share of training rows per bin.}

\item{min_n}{Minimum number of training rows per bin.}

\item{monotone}{\code{"auto"} (direction from the Spearman sign),
\code{"increasing"}, \code{"decreasing"} or \code{"none"}.}

\item{scale}{\code{"mean"} (bin mean in the \code{woe} slot) or \code{"logit"}.}

\item{nthread}{Parallel workers by driver, through the package backend.}

\item{alpha}{Alpha of the PSI critical value in the revalidation.}
}
\value{
An object of class \code{scr_cbins}: \code{fit} (the \code{obwoe}-shaped
object), \code{summary} (one row per driver: \code{feature}, \code{type}, \code{n_bins},
\code{eta2}, \code{direction}, \code{converged}, and after revalidation \code{eta2_holdout},
\code{psi}, \code{psi_flag}, \code{holdout_ok}, \code{holdout_reason}), \code{holdout} (bin
table per driver with train and hold-out means), \code{scale} and \code{target}.
\code{summary} keeps the engine columns (\code{algorithm}, \code{total_iv},
\code{iterations}, \code{error}) and, after revalidation, \code{psi_critical},
\code{psi_flag_adjusted} and \code{pct_unbinned}.
}
\description{
Supervised binning for a bounded continuous target, with the result in
the shape of an \code{obwoe} object, so that the \code{\link[=scr_apply]{scr_apply()}} and \code{\link[=scr_sql]{scr_sql()}}
machinery (\code{OptimalBinningWoE::obwoe_apply()} and
\code{OptimalBinningWoE::obwoe_sql()}) reproduces the bin statistic unchanged.
The \code{woe} slot of every bin carries the target mean of the bin (or its
logit with \code{scale = "logit"}); \code{iv} carries the bin's share of the
between-bin sum of squares, so \code{total_iv} is the eta-squared of the
driver, in \verb{[0, 1]}.
}
\details{
Numeric drivers must not contain missing values: run \code{\link[=scr_triage]{scr_triage()}} (or
impute) first, exactly as the scorecard pipeline does. Categorical
missing values become the level \code{"NA"}, as in the engine. When a
\code{holdout_idx} is given, the frozen bins are revalidated: the hold-out
bin means are recomputed, the PSI of the bin shares is reported with the
sample-size-adjusted critical value, and a driver whose hold-out means
break the training order is flagged \code{UNSTABLE_HOLDOUT}.
}
\examples{
set.seed(1)
d <- data.frame(x = runif(600), g = sample(c("a", "b", "c", "d"), 600, TRUE))
d$y <- pmin(1, pmax(0, 0.2 + 0.6 * d$x + (d$g == "d") * 0.2 + rnorm(600, 0, 0.1)))
cb <- scr_bin_continuous(d, "y", c("x", "g"), train_idx = 1:400, holdout_idx = 401:600)
cb
cb$fit$results$x$bin
cb$fit$results$x$woe    # bin means of y
}
\seealso{
Other irb-ead:
\code{\link[=scr_ead]{scr_ead()}},
\code{\link[=scr_ead_data]{scr_ead_data()}},
\code{\link[=scr_ead_downturn]{scr_ead_downturn()}},
\code{\link[=scr_ead_validate]{scr_ead_validate()}}
}
\concept{irb-ead}
