% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classing.R
\name{scr_coarse_classing}
\alias{scr_coarse_classing}
\title{Coarse classing lab: manual binning and manual variable choice}
\usage{
scr_coarse_classing(
  x,
  features = NULL,
  laplace = 0,
  max_iv_loss = NULL,
  author = Sys.info()[["user"]]
)
}
\arguments{
\item{x}{An object from \code{\link[=scr_select]{scr_select()}}.}

\item{features}{Variables the lab covers. Default: every variable that
reached binning (\code{names(x$fit$results)}), so a variable failed by
screening can be rebinned and forced in with a reason.}

\item{laplace}{Smoothing added to the bin counts when recomputing WOE.
\code{0} (default) is exactly the engine's formula.}

\item{max_iv_loss}{Advisory threshold: a manual bin whose hold-out IV
falls more than this fraction below the optimal one raises
\code{IV_LOSS_VS_OPTIMAL}. \code{NULL} uses \code{config$lab_max_iv_loss}.}

\item{author}{Free text recorded in the ledger.}
}
\value{
An \code{scr_classing} object (the lab), with a print method that
summarises the session: variables touched, before/after IV, verdicts,
reasons, pending proposals and the final choice.
}
\description{
Opens a lab on an \code{\link[=scr_select]{scr_select()}} result. Inside it the analyst inspects
the optimal bins of any binned variable (\code{\link[=scr_classing_view]{scr_classing_view()}}), proposes
new breaks or groupings (\code{\link[=scr_classing_propose]{scr_classing_propose()}}), reads the comparison
against the optimal bins, accepts or discards each proposal with a
mandatory reason (\code{\link[=scr_classing_accept]{scr_classing_accept()}}, \code{\link[=scr_classing_discard]{scr_classing_discard()}}),
chooses the final variable list (\code{\link[=scr_classing_choose]{scr_classing_choose()}}) and commits
everything to a new \code{scr_result} (\code{\link[=scr_classing_apply]{scr_classing_apply()}}) that the rest
of the pipeline consumes unchanged: \code{\link[=scr_scorecard]{scr_scorecard()}}, \code{\link[=scr_apply]{scr_apply()}},
\code{\link[=scr_sql]{scr_sql()}}, \code{\link[=scr_export]{scr_export()}}.
}
\section{Contract of a manual bin}{


A manual bin is recomputed \strong{on the training rows only} (hold-out rows
can never define a bin), with the engine's own WOE formula
(\verb{ln(\%event / \%non-event)}, event-oriented, so glm coefficients stay
positive), then revalidated on the hold-out with the bins frozen (IV, PSI
with both thresholds, unbinned share) and screened with the eight
engine rules, so the lab and the pipeline can never disagree. Numeric
intervals are right-closed, \verb{(a, b]}, exactly as the engine and its SQL.
Re-declaring the optimal cut points of a numeric reproduces the engine's
WOE exactly; for a categorical the engine applies a small internal
smoothing of its own, so the raw log-ratio of the lab differs from it in
the third decimal.
}

\section{What is never allowed silently}{


An empty bin, a degenerate bin (no events or no non-events, unless
\code{laplace > 0}), a bin below \code{lab_min_bin_pct_hard}, a manual IV crossing
\code{iv_max} (the lab must not manufacture leakage), a category left
unassigned, a missing reason. Those block the proposal (\code{BLOCKED});
accepting one needs \code{override = TRUE}, and the override is itself a
ledger row.
}

\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
res <- scr_select(scr_demo, "default", config = cfg, drop = "id",
                  date_col = "ref_date")
lab <- scr_coarse_classing(res)
lab
scr_classing_view(lab, "ds_region")
p <- scr_classing_propose(lab, "ds_region",
                          groups = list(edge = c("NORTH", "SOUTH"),
                                        core = c("EAST", "WEST", "CENTRE")))
p
lab <- scr_classing_accept(lab, p, reason = "edge/core is what pricing uses")
lab <- scr_classing_choose(lab, drop = "vl_score_10",
                           reason = "not available at decision time")
lab
res2 <- scr_classing_apply(lab)
scr_selected(res2)
scr_selected(res2, which = "consensus")
sc <- scr_scorecard(res2)
sc$model_card$binning_algorithm
}
\seealso{
Other classing:
\code{\link[=scr_classing_accept]{scr_classing_accept()}},
\code{\link[=scr_classing_apply]{scr_classing_apply()}},
\code{\link[=scr_classing_choose]{scr_classing_choose()}},
\code{\link[=scr_classing_propose]{scr_classing_propose()}},
\code{\link[=scr_classing_spec]{scr_classing_spec()}},
\code{\link[=scr_classing_view]{scr_classing_view()}},
\code{\link[=scr_decisions]{scr_decisions()}}
}
\concept{classing}
