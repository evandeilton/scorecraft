% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{scr_compare}
\alias{scr_compare}
\title{Compare runs across targets}
\usage{
scr_compare(x)
}
\arguments{
\item{x}{An object from \code{\link[=scr_run]{scr_run()}}, or a named list of \code{scr_result}.}
}
\value{
A \code{data.table} with one row per successful target.
}
\description{
One row per target, with the funnel, the hold-out performance of the best
model (with CI) and the warning signs.
}
\examples{
cfg <- scr_config(verbose = FALSE, nthread = 1, use_ranger = FALSE,
                  xgb_rounds = 60, n_boot = 20)
r1 <- scr_select(scr_demo, "default", config = cfg, drop = c("id", "churn"),
                 date_col = "ref_date")
r2 <- scr_select(scr_demo, "churn", config = cfg, drop = c("id", "default"),
                 date_col = "ref_date")
scr_compare(list(default = r1, churn = r2))
scr_core(list(default = r1, churn = r2), min_targets = 2)
}
\seealso{
Other portfolio:
\code{\link[=scr_core]{scr_core()}},
\code{\link[=scr_run]{scr_run()}},
\code{\link{scr_runset}}
}
\concept{portfolio}
